# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repo state

Next.js (App Router, TypeScript, npm) scaffold at the repo root — landed with spine sub-branch S1 (`feat/spine-ci`) — plus the planning documents (`BUILD_BRIEF.md`, `BUILD_PLAN.md`, `PROMPTS.md`), `DESIGN.md`, per-session handoffs in `handoff/`, the Phase 0 design tokens and static design artifacts in `design/`, and an MIT license. App code so far: the recruiter render in `app/` — `layout.tsx` with the skip link and site header (Home · Resume), `page.tsx` (home: the sections), `[section]/page.tsx` (the section index), `[section]/[slug]/page.tsx` (the entry: dates, markdown body, typed metadata, links, tags, related), `resume/page.tsx`, the shared `entry-dates.tsx`, `site.module.css` — the console shell's components in `app/(explorer)/` (S7: `tiles.ts` icon inlining, `section-tiles.tsx`, the `tile-row.tsx` and `back-key.tsx` client islands, `key-hints.tsx`, the pure `keys.ts`, `explorer.module.css`, tests beside them; S8: `trophy.tsx`, the `status` → trophy-state mapping) — the render helpers in `lib/render/` (S6: `markdown.ts`, the one place raw HTML is produced, and `dates.ts`), `lib/site.ts` (`SITE_NAME`), and the boot-chime port in `lib/audio/` (not wired; the boot sequence is `feat/shell-boot-profile`). The database (S2, `feat/spine-schema`) lives in `supabase/` — migrations, `seed.sql` (fixture), `seed.content.sql` (real content, S3), `config.toml`, and the integration tests in `supabase/tests/` — with `docker-compose.yml` for the local Postgres plus a PostgREST sidecar (S4) and `scripts/db/apply.mjs` as the local/CI migration runner. `scripts/linkedin/` is the S3 one-shot LinkedIn export importer (zero-dependency `.mjs` with JSDoc types, tests beside it). The query layer (S4, `feat/spine-api`) is `lib/db/` (anon supabase-js client, generated `database.types.ts`) and `lib/content/` (Zod schemas per kind, queries, revalidation), tests beside them. The shared route table (S5, `feat/spine-routes`) is `lib/routes/` — `table.ts` (the URL contract: sections, hrefs, `?facet=` parsing) and `load.ts` (what each URL reads, bound to the S4 queries; S6 added `loadResume`) — tests beside them, plus `supabase/tests/routes.test.ts` in the database suite. The one runtime dependency S6 added is `marked` (exact-pinned, zero transitive dependencies), used only by `lib/render/markdown.ts` and never in a client bundle.

- **Commands** (npm):
  - `npm run dev` · `npm run build` · `npm run start`
  - `npm run lint` — ESLint flat config; `design/**` is excluded (self-contained Phase 0 artifacts with their own linter)
  - `npm run typecheck` — `next typegen && tsc --noEmit` (typegen first: the app uses Next's generated route types, e.g. `LayoutProps`)
  - `npm test` — Vitest, all tests. Single file: `npx vitest run lib/audio/boot-chime.test.ts`; single test: add `-t "<test name>"`
  - `npm run tokens:check` / `npm run tokens:build` — verify / regenerate the design-token outputs (see below)
  - `npm run db:up` / `npm run db:down` — start / wipe the local Postgres (`docker-compose.yml`, Supabase's own image, port 54322) and, since S4, the PostgREST sidecar in front of it (port 54321 = `config.toml` `[api] port`; bare PostgREST, no `/rest/v1` prefix, no gateway) · `npm run db:apply` — apply `supabase/migrations/*.sql` then `supabase/seed.sql` and `supabase/seed.content.sql` to `DATABASE_URL` (defaults to the compose DB; re-runnable, applies only new migrations; seeds are idempotent upserts) · `npm run db:reset` — wipe + restart + apply · `npm run db:test` — schema, RLS, content-seed, and query-layer integration tests (needs a migrated DB and the sidecar; not part of `npm test`) · `npm run db:types` — regenerate `lib/db/database.types.ts` from the schema (needs Docker; run after any migration) · `npm run db:psql` — psql shell
  - `npm run linkedin:import -- <export-dir> [--force]` — regenerate `supabase/seed.content.sql` from the owner's LinkedIn export (S3). It reads exactly five profile CSVs, never the rest of the export, and **refuses to overwrite without `--force` because the output is hand-edited** — edit the seed, not the importer, unless a new export arrives. The export folder is never committed (`.gitignore`).
  - CI (`.github/workflows/ci.yml`) runs lint, typecheck, test, tokens:check, build, a Lighthouse budget (`lighthouserc.json`), and the database job (fresh `supabase/postgres` service container + PostgREST sidecar → `db:apply` → `db:test`). The single required status check on `main` and `lane/*` is the aggregate job **`CI green`** — new CI jobs join its `needs` list. The two public Supabase values (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`) are GitHub **repository variables** (`vars.*`, never secrets — the same values Vercel holds) and are passed only to the `build` and `lighthouse` jobs, so a route may prerender against the hosted project. `.github/workflows/keep-warm.yml` reads `reaction_counts` as `anon` once a day from the same variables to keep the Free-plan project from pausing; it is temporary — delete it when Pair 1's ingestion cron lands — and is not in `CI green`'s `needs`.
- **Design artifacts** live in `design/` as self-contained static HTML/SVG opened directly from disk — no build step, no external requests. The Phase 0a identity comparison page is `design/identity-proposals/index.html`; the chosen identity is column C, Ink & paper.
- **Design tokens (Phase 0b)** — `design/tokens/tokens.css` is the source of truth: CSS custom properties, light in `:root`, dark under `:root[data-theme="dark"]` and the `prefers-color-scheme` media query, durations zeroed under `prefers-reduced-motion`. `tokens.json`, `CONTRAST.md` and `specimen.html` beside it are **generated — never edit them**. After editing `tokens.css` or any SVG run `node design/tokens/build.mjs`; before committing run `node design/tokens/build.mjs --check`, which fails on stale output, a contrast pair below AA, or an SVG that breaks the construction rules. `app/layout.tsx` imports `tokens.css` as the global stylesheet — nothing else may define colours, sizes, radii, easings or shadows (`app/app.css` is wiring only, `var()` consumption) — and CI runs `--check` as the `Design tokens check` job.
- **Database (S2)** — schema is brief §4 verbatim; access rules are brief §7. Conventions:
  - Migrations are `supabase/migrations/<YYYYMMDDHHmmss>_<name>.sql`, applied in filename order. A migration that has reached `lane/spine` is never edited — add a new file. Every object gets **explicit** `revoke`/`grant`s (the hosted default no longer exposes new `public` objects to the Data API roles; the local image still does) and RLS is enabled on every table.
  - Access model: `anon` reads all content tables; on `reactions` it may `insert (entry_id, emoji, ip_hash)` only and reads aggregates through the `reaction_counts` view — never raw rows. Writes require `public.is_admin()`, which is true only when the JWT carries `app_metadata.role = 'admin'` (set by the service role / dashboard, never by the user). `service_role` bypasses RLS. Emoji allowlist and the 64-hex `ip_hash` shape are CHECK constraints, so they hold for every writer.
  - `entries.kind` and `entries.facet` are deliberately not enumerated in the database (new section types cost zero migrations); Zod schemas keyed on `kind` (S4) own their values. `status`, `relation_type`, `tags.category`, `links.kind` are closed sets enforced by named CHECKs. Relations read `<from> <relation_type> <to>` (project `part_of` experience, certification `certifies` experience).
  - `supabase/seed.sql` is **fixture data only** (`metadata.fixture = true`), proving the brief §4.1 shape. `supabase/seed.content.sql` is the **real content** (S3): generated once from the LinkedIn export, then owner-edited; every row has a fixed id (never change one), `metadata.source`, and `metadata.date_precision` (LinkedIn gives months, so the day is `01`); relations are written by slug and fail the seed loudly if a slug is missing; `-- REVIEW` marks a field the export could not supply or a judgment to confirm. Both seeds run locally and in CI in that order (`SEED_FILES` in `apply.mjs` = `[db.seed] sql_paths` in `config.toml`), never on production. Honors are `kind = certification` with `metadata.category = "award"` (the trophy case holds both — brief §5).
  - `supabase/tests/rls.test.ts` is the executable statement of the §7 rules — extend it whenever a policy or grant changes; keep its counts fixture-filtered, since real rows load after the fixture. `supabase/tests/content.test.ts` pins the real BTT record's three placements and the content seed's idempotency. `DATABASE_URL` is local-only; hosted credentials go in Vercel/Actions secret stores and never in the repo.
  - **Query layer (S4)** — the app reads only through `lib/content/queries.ts` (`listSection`, `getFacetCounts`, `listRelated`, `getEntryBySlug`, `listTrophies`; contract in `handoff/feat-spine-api.md`), which reads via supabase-js over PostgREST as `anon` with the two public `NEXT_PUBLIC_SUPABASE_*` values — never a database connection string in app code. `lib/content/schema.ts` is the **single source of the `kind` / `facet` value sets** (brief §4) and of each kind's metadata keys; a row that fails it throws (`ContentValidationError`) rather than disappearing. Every list requests `count: "exact"` and throws if PostgREST truncated it at `max_rows`. Caching is the fetch Data Cache (`next: { revalidate: 3600, tags: ["content", "content:<table>"] }` added in `lib/db/client.ts`), revalidated through `lib/content/revalidate.ts`; a route that reads at prerender inherits that window unless it declares its own — no `cacheComponents`, no `'use cache'`. `supabase/tests/api.test.ts` exercises the layer end to end against the sidecar; extend it when a query shape changes.
  - **Route table (S5)** — `lib/routes/table.ts` is the URL contract (its header comment is the table; handoff in `handoff/feat-spine-routes.md`): `SECTIONS` in brief §4.3 order (`/experience` · `/projects` · `/certifications` = the trophy case · `/education` · `/hobbies` = kinds `hobby` + `interest` · `/now` = kind `post`), an entry's canonical URL is `/<section of its kind>/<slug>` (`entryHref`; `sectionForKind` is total over `KINDS` and checked at import), facet chips are `?facet=<facet>` (`parseFacetParam`; invalid → 404), `/resume` is linked from the site header on every page, `/privacy` and `/admin` are reserved constants with no page. Adding a section is one `SECTIONS` entry, zero new route files. `lib/routes/load.ts` is what each URL reads (`loadSection`, `loadEntry`, `loadTrophies`, and S6's `loadResume`) through an injectable `queries` object typed against `lib/content/queries.ts`; loaders return discriminated results (`found` / `redirect` / `not-found`) and never import `next/navigation` — only page files call `notFound()` / `permanentRedirect()`, at the top level of the page, outside any `try`. One page file per URL (`app/[section]/page.tsx`, `app/[section]/[slug]/page.tsx`); a slug reached under the wrong section 308s to its canonical URL. Rendering modes: `/[section]` reads `searchParams`, so it renders per request (the fetch Data Cache still bounds database reads to hourly); `/[section]/[slug]` declares `generateStaticParams → []` and is SSG-on-demand under the inherited 3600 s window and `content` tags; `/` is static. **Since S6, `/resume` reads content, so it prerenders at build under the same 3600 s window (`○` with a `1h` revalidate column) and `next build` therefore needs the two `NEXT_PUBLIC_SUPABASE_*` values** — a fresh clone without them cannot build, and a hosted-project outage fails the build rather than a request. `lighthouserc.json` audits `/`, `/experience`, `/experience/break-through-tech` and `/resume`.
  - **Recruiter render (S6)** — handoff in `handoff/feat-spine-recruiter.md`. Every page is server-rendered semantic HTML and works with JavaScript disabled, which is a spine acceptance item, not a nicety. (Until S7 that was enforced by there being no `"use client"` in the repo at all; S7 added two islands and the property is now held by *what* they do — see Console shell below — and proved by the script-stripping render, not by a grep.) `lib/render/markdown.ts` is the **only** thing allowed to produce raw HTML — `renderMarkdown(body)` escapes every embedded tag, allowlists hrefs to `http(s)` and site-relative (a bare leading `//` is rejected as off-site), drops non-`http(s)` images, and re-levels headings so the body's shallowest is an `h2` under the page's one `h1`. It is server-only; importing it from a client component would ship `marked` to the browser. Every date goes through `lib/render/dates.ts`, which uses **no `Date` objects** and honours `metadata.date_precision` — a month-precision row renders "May 2026" and `<time datetime="2026-05">`, never the seed's placeholder day. `/resume` is entries only: no contact details anywhere in the tree (a contact line is `feat/recruiter-resume-print`'s, with owner-supplied values). No media markup ships until a Storage bucket exists (`feat/admin-media`).
  - **Console shell (S7)** — handoff in `handoff/feat-spine-console-tile.md`. `app/(explorer)/` is the shell's **component directory, not a route tree**: it holds no `page.tsx` and no `layout.tsx`, because two route groups resolving to one path is a build error and the architecture fixes one page file per URL. The tile row is the *enhanced rendering of the home page's existing section nav* — the same `<ul>` of `<a>`s, in `SECTIONS` order — so there is no mode switch and no second markup tree; mode is not a URL dimension. Tiles carry no counts, which is what keeps `/` a `○` static route that reads no content; a section with no entries keeps its tile and shows its own empty state on `/<section>`, rather than being hidden and made unreachable. Conventions:
    - **Two client islands, both tiny, both focus/keys only:** `tile-row.tsx` (roving `tabindex`) and `back-key.tsx` (Escape). They receive **server-rendered children**, never content props, so no label, href or SVG reaches the browser bundle. An island must not import `lib/content/*`, `lib/render/*`, `lib/db/*` or `zod` (they would ship `marked`/`zod` to the client), and must not call `useSearchParams()` (it would cost `/` its `○`). Every pure key rule lives in `keys.ts`, which imports nothing — that is the only part testable under Vitest's `node` environment.
    - **The roving `tabindex` is applied after hydration, never in the markup.** The server sends six ordinary links, so JS-off leaves six working tab stops; the island then demotes all but one. Its effect deliberately has **no dependency array** so a soft navigation that replaces the anchors cannot silently restore six tab stops.
    - **Enter is never handled in JS** — the tiles are real `<a>`s and already activate on Enter (brief §2.2's "Enter = A"). A modified arrow (`Alt`/`Cmd`+Arrow is browser back) is never consumed; `nextIndex` returns `null` for it.
    - **Icons are inlined from `design/assets/icons/` by `(explorer)/tiles.ts`**, which ports `design/tokens/build.mjs`'s `inline()`: strip `id` / `role` / `aria-labelledby` **and the `<title>`**, add `aria-hidden="true"`. Stripping is load-bearing — the source files carry hard-coded ids, and a retained `<title>` would give each tile link a second accessible name. The label must stay a **sibling** of the icon span, never inside it. `now` is drawn by `tile-news.svg` (brief §4.3's "Now/News").
    - **`next.config.ts` names `design/assets/icons/**` in `outputFileTracingIncludes`** for `/[section]` and `/[section]/[slug]`. Those routes read the icons at cold start inside the serverless function, and nothing local can reveal a tracing gap because `next start` and Lighthouse both run from the repo root. Any new per-request route that inlines an asset needs the same entry.
  - **Trophy case (S8)** — handoff in `handoff/feat-spine-trophy.md`. `/certifications` (`trophyCase: true`) is the S6 index row **plus a state**, not a second rendering: `app/(explorer)/trophy.tsx` owns the mapping and `app/[section]/page.tsx` keys one branch on `section.trophyCase`. Conventions:
    - **`archived` renders as "Archived", never "Locked"** — three statuses, three states, three drawings, one to one (`TROPHY_LABELS`). An archived credential is one you earned that lapsed, not one you never achieved. **`supabase/seed.sql`'s fixture comment is stale on this point**; this bullet and `trophy.test.tsx` are the authority. `TROPHY_LABELS` is `Readonly<Record<Status, string>>`, so a fourth `status` fails `typecheck` here until someone decides what it looks like.
    - **State is always text.** `<TrophyState>` renders a `<span>` (it nests inside the meta line's `<p>`; a `<p>` in a `<p>` is invalid) whose label is a **sibling text node** of the `aria-hidden` glyph — never colour or icon alone (WCAG 1.4.1, brief §2.2). `TROPHY_ICONS` in `(explorer)/tiles.ts` is keyed by `status`; `archived` is drawn by `trophy-locked.svg`, the same file-vs-meaning mismatch as `now` → `tile-news.svg`. `trophy-unlocked.svg` carries the set's only literal colour, in the sanctioned `var(--color-primary, #E8B11B)` form, inside the path's own stroke — DESIGN.md's outline rule met by the drawing, not a wrapper.
    - **Adding a class to `explorer.module.css` costs client bytes.** `tile-row.tsx` is a client island and imports that stylesheet, so its CSS-module class-name map ships in the browser bundle (S8's two classes = its whole +15 B script delta). The drawings never ship.
  - Hosted project: the CLI is linked from the repo root (`supabase/.temp/`, ignored) to the production project. **Migrations reach it through the Supabase GitHub integration** (working directory `.`, production branch `main`, deploy-to-production on) when `lane/spine` merges to `main` — do not `db push` by hand; the seed never runs on the hosted database automatically. **Loading `supabase/seed.content.sql` into the hosted project is a manual chore, repeated after every owner seed edit:** `psql "$DATABASE_URL" -f supabase/seed.content.sql` with `DATABASE_URL` parsed from the git-ignored `.env`, followed by a read-only, allowlisted count check (per kind, the three brief §4.1 ids, relations, links) recorded in that chore's handoff; the seed is idempotent, so a re-run only applies the edits. Hosted service settings come from `supabase/config.toml` via `npx supabase@latest config push` (Free plan: Pro-only features such as vector buckets are pinned off in the file, or the push 402s). Supabase Branching is not used (Free plan — settled); the CI database job is the per-PR isolation. Credentials live only in the git-ignored `.env` locally and in Vercel/Actions secret stores; never `source .env` (its `KEY =value` lines would execute), parse it.
- **SVG assets** — `design/assets/icons/` (24 px grid, 3 px stroke, square caps, mitred joins, `currentColor`; a fill only for the active/unlocked item), `design/assets/mark/` (KJ monogram and badge), `design/assets/chime/` (boot chime as a Web Audio spec in code; no audio file). A literal colour in an SVG is allowed only as `var(--color-*, #fallback)` with the token's light value as the fallback — the generator enforces it. Font files (Inter, JetBrains Mono) are never committed; the app loads them at build time. `DESIGN.md` is the provenance record and stays free of source URLs.
- The `.gitignore` started as a Python template; S1 appended a Node/Next.js block (`node_modules/`, `.next/`, `.vercel`, `.env*`, `.lighthouseci/`) and removed the template's bare `lib/` entry, which would have ignored application code under `lib/`. The `REFERENCES.md` and `launch.json` entries stay.

## Authority documents — read before doing anything

| File | Authoritative on |
|---|---|
| `BUILD_BRIEF.md` | *What*: scope, constraints, requirements, data model, stack |
| `BUILD_PLAN.md` | *When / where*: phase order, branch names, merge gates, acceptance checklist |

Where they disagree on order, branching, or lane composition, the plan wins. Scope decisions are settled — do not relitigate them. Brief §8 is superseded by plan §3–6; do not follow its ordering. Flag genuine blockers; ask before inventing content (entries, dates, orgs, team names). Items the owner must supply, and which phase each blocks, are tabulated in plan §8.

**`REFERENCES.md` — local only, never committed.** A working file that records where the interaction grammar in brief §2.1 and §5 was observed, annotated against the sub-branches each source informs. It is authoritative for interaction-grammar questions in Phase 0 and `lane/console-shell`. It is git-ignored by design: committing a list of source URLs would turn `DESIGN.md` from a provenance document into a side-by-side comparison exhibit, which undercuts the borrowed-grammar argument the IP posture rests on. Expect it to be **absent in a fresh clone** — that absence is normal, is not a blocker, and is not a reason to re-derive the references. It also carries a caution worth repeating here: the illustrative-use rationale that ticoverse.com applies to third-party console logos and box art **does not transfer to this project**. Brief §2.1 governs assets without exception.

## Architecture (as specified)

**Two renderers, one dataset, same URLs.**
- *Recruiter mode* — SSR, semantic HTML, works with JS disabled, canonical for SEO, default for `/`. A plain `/resume` route is reachable in one action from anywhere.
- *Explorer mode* — handheld-console shell (tile row, D-pad/keyboard/gamepad navigation, trophy case, play-activity charts, toasts). An enhancement over the working site, never the only way in.
- Both bind to the **shared route table** (spine S5, `lib/routes/table.ts`), written before either renderer existed. Every tile resolves to a real, shareable URL; no state-only navigation. Mode is not a URL dimension: the same URL serves both renderers through **one App Router page file per URL** (two route groups resolving to one path is a build error), which is the seam the renderers plug into.
- Ownership: the shell lives in `app/(explorer)`, admin in `app/admin`. Shared-component changes are raised before editing.

**Polymorphic `entries` table** (`kind` × `facet`) is the core; new section types must cost zero migrations. Zod schemas keyed on `kind` recover type safety at the app layer. `entry_relations` (`part_of` / `certifies` / `produced_by` / `related_to`) give multi-placement: the Break Through Tech AI worked example (brief §4.1) is one `experience` record that surfaces in Experience, Projects, and the trophy case with zero duplication — every consumer of the data must handle this. `status` (`unlocked` / `in_progress` / `archived`) drives trophy states. Facet-chip counts come from the query, never hardcoded.

**Ingestion is scheduled, not live-proxied.** GitHub Actions cron workers (Spotify, Steam, IGDB) normalize into Postgres; the site only ever reads its own DB and serves last-known-good on upstream failure. Workers are idempotent upserts with retry/backoff and structured logging, and fail loudly as Actions failures. The LinkedIn import is a one-shot pre-spine importer (S3), not a scheduled worker — there is no LinkedIn API, and scraping is off the table.

**Auth is admin-only** (Supabase Auth, one user, guards `/admin`). **Reactions are anonymous emoji only:** fixed server-side allowlist, salted `ip_hash` (never raw IP), edge rate limit, RLS allowing anon `insert` + aggregate `select` only.

**Stack** (decided, brief §3): Next.js App Router + TypeScript · Vercel Hobby · Supabase Postgres / Auth / Storage · GitHub Actions cron (not Vercel Cron) · Zod at every boundary · cookie-free analytics. Cost target $0 + domain. Never commit binaries; tile art and screenshots go to Supabase Storage.

## Non-negotiable constraints

- **IP (brief §2.1):** borrow interaction grammar only. No Nintendo / Disney / Capcom / Square Enix assets, no Joy-Con silhouettes or red/blue pairing, no Switch wordmark, chime, or typefaces, no rendered console hardware, and the phrase "Nintendo Switch" nowhere in UI copy, metadata, filenames, or alt text. Original wordmark, palette (not red/blue), hand-drawn SVG icons, original chime. `DESIGN.md` must record the borrowed-grammar vs. protected-expression distinction.
- **Accessibility (brief §2.2):** full keyboard operation (arrows navigate, Enter = A, Escape = B, roving `tabindex`, visible focus ring, no traps), semantic HTML beneath the shell, `prefers-reduced-motion` disables boot / zoom / parallax, sound off by default, WCAG 2.2 AA in both themes, touch targets ≥44px.
- **Privacy (brief §2.3):** no visitor accounts, no email collection, no fingerprinting, no session replay, no IP-to-company enrichment. Ship `/privacy`.

## Workflow

**Branching (plan §2):** `main` is protected. `lane/<name>` is long-lived, one per lane, rebased on `main` after every `main` merge. `feat/<lane>-<slice>` is a stacked sub-branch that merges into its lane. Sub-branch → lane auto-merges on green CI with no human step (Phase 0 predates CI and merges by hand — plan §3); lane → `main` needs green CI **and** the owner-signed acceptance checklist (plan §7, including its security and IP blocks) — that is the only human merge gate. One agent per lane; never two agents on one lane branch. Every PR body links the lane checklist.

**Commit attribution (owner-only):** every commit is the owner's own work product. Author **and** committer must be the identity in `git config user.name` / `user.email` — the one exception being a squash merge, whose committer is GitHub's own web-flow identity. No `Co-Authored-By` trailer naming an assistant, no "Generated with" footer, and no assistant, model or vendor credited as author, co-author or generator in a commit message, PR title or PR body. This is a rule about *attribution*, not a banned-substring list: naming the file `CLAUDE.md` in a subject line is fine. Verify after every commit — `git log -1 --format=%B`, `git log -1 --format='%(trailers)'` (**must be empty**; the trailer check is the one that actually catches a slip, since a trailer does not appear in a casual read of the subject), and `git log -1 --format='%an <%ae> | %cn <%ce>'`. This repo has no hook, no `commit.template` and no `commit.gpgsign`, so a trailer can only come from the message you write.

**Stopping:** stop when the PR opens — see `BUILD_PLAN.md` §2.1, which is canonical for stopping behavior and is not summarized here.

**Phase order (plan §3–6):** Phase 0 identity (0a proposes three complete identities → **hard stop for the owner's pick** → 0b tokens, icons, wordmark, chime, `DESIGN.md`) → Phase S spine (S1–S8, serial, on `lane/spine`) → Phase 1 lane pairs, two concurrent maximum, paired for file disjointness (recruiter + ingestion → console-shell + admin → reactions + content) → Phase 3 hardening. Nothing visual starts until 0b merges.

**Verify at build time, not from memory (plan §9):** Vercel Hobby non-commercial terms, Supabase free-tier pause behavior and the nightly keep-warm assumption, Spotify / Steam / IGDB API access terms, IGDB cover-art usage rights.
