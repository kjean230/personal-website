# Handoff — chore/spine-s4-closeout

**Merged into:** `lane/spine`
**Plan row:** none — a chore closing S4 (PR #12) loose ends before S5. No plan row, no app code, no schema change, no seed change, no new dependencies.

## Shipped
- **Merged S4 verified on a fresh database (2026-08-26).** `lane/spine` = `72e89e4` (PR #12 squash-merge, CI green). `npm run db:reset` brought up both services (PostgREST healthy before the migrations ran), applied both migrations, `seed.sql`, then `seed.content.sql`; `npm run db:test` **54/54** (31 RLS + 9 content + 14 API); `npm test` **68/68**; `npm run lint` 0 problems; `npm run typecheck` clean; `npm run tokens:check` ok; `npm run build` ok (3 static routes).
- **`supabase/seed.content.sql` is byte-identical to PR #10** (`git diff ad1dea6 -- supabase/seed.content.sql` empty) — the owner has not edited it. Consequently no CHECK-constraint or Zod finding to report, no seed re-run beyond the fresh reset above, and **no hosted load: the hosted project stays at 0 content rows.**
- **Hosted smoke through the app's normal read path (2026-08-26).** A throwaway Vitest script in the session scratchpad parsed only `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` from the git-ignored `.env` (never sourced, never printed), built the client with `createAnonClient()` — env-driven, exactly as the app will — and called `listSection("experience")` and `getFacetCounts("experience")` from `lib/content` against project `zfsslawwrgyfkupawrnm`: **HTTP 200, 200; 0 rows; counts `{ all: 0, byFacet: all 0, unfaceted: 0 }`** — the unloaded-project expectation, and the first end-to-end proof that the publishable key, the URL, supabase-js and the caching fetch wrapper work against hosted (S4 had not read hosted). Script deleted; nothing from it committed.
- **Secret / PII grep over `origin/lane/spine` and the working tree, case-insensitive, reported in full** (working tree = every file outside `node_modules`, `.next`, `.git`, `.vercel`, `supabase/.temp`, `supabase/.branches`, `.env*`; paths only): `gmail`, `@glic`, `@fordham.edu`, `linkedin.com/in/` — each **1 file**, `handoff/chore-spine-s3-closeout.md`, the previous chore's own grep paragraph (the search terms, no values) · `sb_secret` **0** · legacy JWT key prefix `eyJhbGci` **0** · `service_role` — role *name* only: `CLAUDE.md`, `docker-compose.yml`, `handoff/chore-spine-hosted-apply.md`, `handoff/feat-spine-schema.md`, `supabase/migrations/20260825010100_spine_rls.sql` (10, the grants), `supabase/tests/rls.test.ts` — no key value anywhere · **the hosted `DATABASE_URL` password** (extracted in-process from `.env`, compared by fixed string, never printed): **0** tracked files, **0** unignored working-tree files · `nintendo` — the constraint text and its enforcement only: `BUILD_BRIEF.md` (4), `BUILD_PLAN.md` (2), `CLAUDE.md` (1), `PROMPTS.md` (2), `design/tokens/build.mjs` (`PROHIBITED_TERMS`), `scripts/linkedin/import.mjs` (output guard), `scripts/linkedin/sql.test.ts` (assertion), `handoff/chore-spine-s3-closeout.md` (its grep paragraph) · `joy-?con` — `BUILD_BRIEF.md`, `BUILD_PLAN.md`, `CLAUDE.md` (the constraint), `design/tokens/build.mjs` (enforcement). Zero occurrences of any term in UI copy, metadata, filenames, alt text, seed content, or `lib/`. (Once this handoff merges the same grep matches this paragraph — the terms, not values.)
- **Handoff annotations:** `handoff/feat-spine-linkedin-import.md` and `handoff/chore-spine-s3-closeout.md` "Next" sections now read **"Done 2026-08-26 (PR #12), see `handoff/feat-spine-api.md`."** Nothing in `handoff/feat-spine-api.md`'s Deferred or Open-questions lists was resolved by this chore (every owner input was left unfilled — below), so every item there stays visibly open and none is struck through.
- **CLAUDE.md unchanged** — no convention was added or changed (no repository variables, no keep-warm, TTL kept).
- Working-tree hygiene: nothing tracked touched except the two handoff annotations and this file; `.env*`, `REFERENCES.md`, `.vercel/`, `supabase/.temp/` present locally, ignored, unstaged, values unread beyond the two public keys named above.

## Deviated from plan
- None against the plan. Against the task text: all six owner-input lines were left in brackets. Resolved per the task's own rule — `SEED EDITS DONE` from the repository (seed unchanged → **none**), which forces `HOSTED LOAD` **no**; the rest as "no / keep / leave" — and each untaken decision is recorded below rather than guessed. Items 2, 4, 5, 6 and 7 were therefore **skipped, not improvised**.
- First pass of the `joy-con` grep used a pattern that matched a literal `?` (only `build.mjs`'s own regex); re-run with the intended `joy-?con` — the results above are the corrected run.

## Deferred
Each with the owner input that left it open:
- **The twelve seed edits** (`handoff/feat-spine-linkedin-import.md` "Open questions") and **the one-time hosted load** — `SEED EDITS DONE` unfilled → none; `HOSTED LOAD` → no. Hosted has 0 content rows; every renderer (S6 preview onward) shows an empty site until then. The load command is unchanged: `psql "$DATABASE_URL" -f supabase/seed.content.sql` with `DATABASE_URL` parsed from `.env`, then an allowlisted count check.
- **Content cache window 3600 s** — `CACHE TTL` unfilled → keep. `CONTENT_REVALIDATE_SECONDS` in `lib/db/client.ts` is the one constant.
- **CI repository variables** (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` as GitHub repository *variables*, wired into the `build` and `lighthouse` jobs) — `CI PUBLIC VARS` unfilled → no. **Consequence for S5/S6:** the CI build has no Supabase environment, and `createAnonClient()` throws without it, so a route that reads content while prerendering would fail `npm run build` in CI. Until the variables exist, content routes must render dynamically (or be added to CI first — a one-line `gh variable set` each plus `env: vars.*` on two jobs).
- **Keep-warm workflow** — `KEEP-WARM` unfilled → no. The hosted project (created 2026-08-24) still has no regular traffic; today's smoke was a handful of reads. Expect the Free-plan pause after ~7 days of low activity (one-click restore); Pair 1's ingestion cron resolves it permanently. The proposed one-file workflow stands in `handoff/chore-spine-hosted-config.md` open question 1.
- **BUILD_PLAN §7 Ops / brief §9 wording** — `PLAN §7 OPS WORDING` unfilled → leave. Proposed text in `handoff/chore-spine-hosted-config.md` open question 2.
- Out of this chore's scope by the task, unchanged from `handoff/feat-spine-api.md`: `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.

## Open questions for owner
The decisions still unmade — the same six inputs, restated:
1. Edit the seed (twelve items) and say so; then the hosted load is a one-command chore, or run it yourself as above.
2. Keep the 3600 s content cache window, or name a different number of seconds.
3. **S5 needs this one before it plans the route table:** add the two public `NEXT_PUBLIC_SUPABASE_*` values as GitHub repository variables (so S6 can prerender in CI), or direct S6 to render content routes dynamically.
4. Keep-warm workflow before Pair 1, or accept the pause.
5. Apply the §7 Ops / brief §9 wording, or leave it.

## Next
S5 — `feat/spine-routes` on `lane/spine`: the shared route table both renderers bind to, written against the S4 contract (`listSection` per section, `getEntryBySlug` per entry URL, `getFacetCounts` for chips, `listTrophies` for the trophy case). Not started. **Two routing inputs S5 needs, stated plainly:** (a) **prerender vs dynamic** — as of this chore CI has no `NEXT_PUBLIC_SUPABASE_*` variables, so content routes must be planned as **dynamic** unless the owner adds the variables first (open question 3); (b) **the inherited ISR window** — the 3600 s fetch-cache window **stands** (TTL kept), so any route that does read content at prerender inherits `revalidate ≤ 3600` unless it declares its own segment config.
