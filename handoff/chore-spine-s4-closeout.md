# Handoff — chore/spine-s4-closeout

**Merged into:** `lane/spine`
**Plan row:** none — a chore closing S4 (PR #12) loose ends before S5. No plan row, no app code, no schema change, no seed content change, no new dependencies.

This chore ran twice on the same branch name. **Pass 1 (PR #13, merged `6f73d40`)** was issued with every owner input unfilled and resolved them all as "no / keep / leave" per its task rule. **Pass 2 (this PR)** was issued with the inputs settled — cache TTL keep 3600; CI repository variables add; keep-warm add; §7 Ops wording apply; seed edits items 1, 3, 9, 11 + hosted load — and did everything those inputs turned on, except the seed-dependent items, because the seed edits were not in the repository (below).

## Shipped

### Pass 2 (2026-08-26)
- **Seed edits not found — hosted load not done.** The task said the owner's edits (items 1, 3, 9, 11) were uncommitted in the working tree, with the rule "if `git diff ad1dea6 -- supabase/seed.content.sql` is empty, treat as none and HOSTED LOAD as no, and say so." It was empty: the working tree was clean, `supabase/seed.content.sql` byte-identical to PR #10, no stash. **Treated as none; the hosted project stays at 0 content rows.** Nothing was loaded and no seed commit was made. If the edits exist somewhere other than this checkout (unsaved editor buffer, another clone), they are not in git.
- **Merged S4 re-verified on a fresh database** at `lane/spine` = `6f73d40`: `db:reset` (both services; PostgREST healthy before the migrations), `db:test` **54/54** (31 RLS + 9 content + 14 API), `test` **68/68**, lint 0, typecheck clean, `tokens:check` ok, `build` ok.
- **CI repository variables.** `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` set with `gh variable set` from the values parsed out of the git-ignored `.env` (public values; the key's `sb_publishable_` prefix was asserted before setting; nothing printed). `gh variable list` shows exactly those two names; `gh secret list` is empty. `.github/workflows/ci.yml` passes them as `env: vars.*` to the `build` and `lighthouse` jobs only — so a route that reads content while prerendering can build in CI.
- **Keep-warm workflow** `.github/workflows/keep-warm.yml` — daily cron `17 9 * * *` + `workflow_dispatch`, one job, one `curl --fail` of `$SUPABASE_URL/rest/v1/reaction_counts?select=entry_id&limit=1` with `apikey` and `Authorization: Bearer` from the same repository variables; `permissions: {}`; not in `CI green`'s `needs`; file header says Pair 1's ingestion cron makes it redundant and it should be deleted then. **Dispatched once on this branch: run 33018329947, conclusion `success`** (step `curl … reaction_counts` succeeded). Two caveats: (1) the first push failed to register the file — the plain-scalar `run:` contained `apikey: `, which YAML reads as a nested mapping — fixed with a block scalar in a follow-up commit; (2) **GitHub runs `schedule` triggers only from the default branch**, and the Actions registry names a workflow from its default-branch copy, so the daily read starts (and the name becomes "Keep warm") only when `lane/spine` merges to `main`. Until then the pause protection is the manual dispatch or ordinary traffic.
- **BUILD_PLAN §7 Ops and brief §9 wording applied** — one line each, exactly the text proposed in `handoff/chore-spine-hosted-config.md` open question 2: *"Green CI including the database job (migrations + RLS tests on a fresh database per run); preview deploy live. Supabase Branching is not used (Free plan); previews share the hosted project."* and *"Preview deploy per PR; database isolation is the CI database job."* Nothing else in either file changed.
- **Handoff annotations:** `handoff/feat-spine-api.md` — Deferred "keep-warm and §7 wording" and "S6 build environment" struck through as done; Open questions 1 (TTL confirmed, 3600 kept) and 2 (repository variables) struck through as done; question 3 (seed edits / hosted load) annotated as still open. `handoff/chore-spine-hosted-config.md` — Deferred keep-warm and Open questions 1–2 marked done. **Left visibly open, on purpose:** `handoff/chore-spine-s3-closeout.md` (seed edits, hosted load) and `handoff/feat-spine-linkedin-import.md` (all twelve items, including 1, 3, 9, 11) — none of those edits reached the repository.
- **CLAUDE.md:** the CI bullet now records the two repository variables (public, `build` + `lighthouse` only) and the keep-warm workflow (temporary, delete with Pair 1, outside `CI green`); the hosted bullet records that loading the content seed is a manual chore repeated after every seed edit, with the command and the read-only count check.
- **Secret / PII grep over `origin/lane/spine` (`6f73d40`) and the working tree, case-insensitive, in full** (working tree = every file outside `node_modules`, `.next`, `.git`, `.vercel`, `supabase/.temp`, `supabase/.branches`, `.env*`): `gmail`, `@glic`, `@fordham.edu`, `linkedin.com/in/` — only `handoff/chore-spine-s3-closeout.md` and this handoff's pass-1 paragraph (the search terms) · `sb_secret`, legacy JWT prefix `eyJhbGci` — only this handoff (the terms) · `service_role` — role *name* only (`CLAUDE.md`, `docker-compose.yml`, two S2 handoffs, the RLS migration's grants ×10, `rls.test.ts`, this handoff) · **the hosted `DATABASE_URL` password** (extracted in-process, compared by fixed string, never printed) — **0 tracked, 0 unignored working-tree files** · `nintendo` — the constraint text and its enforcement (`BUILD_BRIEF.md` ×4, `BUILD_PLAN.md` ×2, `CLAUDE.md`, `PROMPTS.md` ×2, `design/tokens/build.mjs`, `scripts/linkedin/import.mjs`, `scripts/linkedin/sql.test.ts`) plus the two closeout handoffs' grep paragraphs · `joy-?con` — `BUILD_BRIEF.md`, `BUILD_PLAN.md`, `CLAUDE.md` (constraint), `design/tokens/build.mjs` (enforcement), this handoff. Zero occurrences in UI copy, metadata, filenames, alt text, seed content, or `lib/`.

### Pass 1 (PR #13, 2026-08-26) — kept for the record
- Merged S4 verified on a fresh database at `72e89e4` (db:test 54, test 68, lint, typecheck, tokens:check, build green).
- Seed unchanged since PR #10 → no CHECK/Zod findings, no hosted load.
- Hosted smoke through the app's normal read path (throwaway script, only the two public `.env` values read, deleted afterwards): `createAnonClient()` → `listSection("experience")` and `getFacetCounts("experience")` against project `zfsslawwrgyfkupawrnm` — **HTTP 200, 200; 0 rows; counts all 0**, the unloaded-project expectation and the first end-to-end proof of the publishable key + supabase-js + caching fetch against hosted. Not repeated in pass 2 (hosted unchanged).
- Secret/PII grep (same terms) clean; "Next → S4" annotated done in the S3 handoffs; every owner input recorded as an untaken decision.

## Deviated from plan
- None against the plan. Against the task text: the **seed commit, hosted load (item 2) and hosted smoke (item 3) were skipped** by the task's own empty-diff rule; the **keep-warm dispatch** needed a YAML fix first (above) and its `schedule` cannot fire until the file is on `main`; and the branch name `chore/spine-s4-closeout` was reused after PR #13 had merged and auto-deleted it (recreated from `lane/spine` at `6f73d40`; the stale local branch, whose content PR #13 had squash-merged, was deleted), so this handoff file is the combined record of both passes rather than a second file.
- Item 7 asked to strike items 1, 3, 9, 11 in `handoff/feat-spine-linkedin-import.md` and the seed/hosted lines in `handoff/chore-spine-s3-closeout.md`; they were **not** struck, because those edits are not in the repository.

## Deferred
- **The twelve seed edits** (`handoff/feat-spine-linkedin-import.md` "Open questions", all still open — including 1, 3, 9, 11) **and the one-time hosted load.** Hosted has 0 content rows; every renderer shows an empty site until then. Load command unchanged (CLAUDE.md, Database bullet).
- Out of scope by the task, unchanged from `handoff/feat-spine-api.md`: `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- Delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands (`feat/ingest-scheduling`).

## Open questions for owner
1. **Where are the seed edits?** They were not in this checkout. Commit them (or say where they are) and the hosted load is a one-command chore; until then the site is empty.
2. Until `lane/spine` merges to `main`, keep-warm's daily schedule does not run. If the project should stay awake before then, dispatch it by hand from the Actions tab (`gh workflow run keep-warm.yml --ref lane/spine` once this PR merges), or accept the one-click restore.

## Next
S5 — `feat/spine-routes` on `lane/spine`: the shared route table both renderers bind to, written against the S4 contract (`listSection` per section, `getEntryBySlug` per entry URL, `getFacetCounts` for chips, `listTrophies` for the trophy case). Not started. **The two routing inputs S5 now has, stated plainly:** (a) **prerender is possible in CI** — the `build` and `lighthouse` jobs carry the two public Supabase variables, so a content route may prerender against the hosted project (which is empty until the seed is loaded — an empty prerender, not a failed one); (b) **the inherited 3600 s ISR window stands** (TTL confirmed) — a route that reads content at prerender gets `revalidate ≤ 3600` unless it declares its own segment config.
