# Handoff — chore/spine-hosted-seed

**Merged into:** `lane/spine`
**Plan row:** none — the manual hosted load CLAUDE.md prescribes after every owner seed edit (Database bullet), run once for the S3 content decisions. No app code, no schema change, no migration, no new dependencies. The seed edit itself is commit `832228e`, merged as `14be8b5` (PR #15). This chore ran across two sessions: the first pushed, loaded, and smoked; the second re-verified the hosted state first-hand, re-ran the idempotency check and the smoke, applied the handoff annotations, and opened this PR.

## Shipped
- **Push path (2026-08-27).** `git push origin lane/spine` was declined by the repository rule (`GH013`: required status check "CI green" is expected on `lane/*`). Not forced. `chore/spine-seed-content` was created from `832228e`, pushed, and opened as **PR #15** into `lane/spine` with auto-merge (squash) and the §7 checklist link in the body; CI green (Build, CI green, Database migrations & RLS, Design tokens check, Lighthouse budget, Lint, Test, Typecheck, Vercel preview) and **merged 03:36 UTC as `14be8b5`**. `git diff 832228e origin/lane/spine` is empty — the squash carries the identical tree. This handoff travels the same way (sub-branch `chore/spine-hosted-seed` from `14be8b5`, auto-merge PR).
- **Hosted load (2026-08-27 04:02 UTC), project `zfsslawwrgyfkupawrnm`.** Transport differed from the documented command, for two reasons found at build time: `psql` is not installed on this machine, and the direct host `db.<ref>.supabase.co` resolves to an IPv6 address only, which the Docker VM cannot route (`Network is unreachable`; the Mac itself reaches it over IPv6). The load therefore ran through the compose `db` container's `psql` 17.6 over the **session pooler** (`aws-0-us-west-2.pooler.supabase.com:5432`, user `postgres.<ref>`, IPv4 — the host/user form recorded in the CLI's ignored `supabase/.temp/pooler-url`) with the same password: the URL was assembled in-process from the git-ignored `.env` `DATABASE_URL` (parsed, never sourced), passed to the container as an environment variable, never printed; the seed went in on stdin with `-v ON_ERROR_STOP=1`. `supabase/seed.sql` (fixture) was not loaded. **Baseline before the load:** entries 0, entry_relations 0, links 0, tags 0. **Load:** `INSERT 0 19` · `INSERT 0 3` · `INSERT 0 1`, exit 0.
- **Allowlisted count check (read-only, every number), run after the load and again by the second session (identical both times):** per kind — certification **5**, education **2**, experience **10**, project **2** (total 19) · the three brief §4.1 ids — `0e02f978…` break-through-tech experience / research / in_progress; `64517535…` airbnb-superhost-classifier project / coursework / unlocked; `d9a232aa…` machine-learning-foundations certification / null / unlocked · entry_relations — part_of **2**, certifies **1** · links **1** · `metadata ? 'fixture'` **0** · `summary is null` **0** · per facet — null 7, research 5, classroom 3, coursework 2, corporate 1, volunteer 1 · featured **4** · in_progress **3** · tags 0, reactions 0 · rows with `updated_at <> created_at` **0** (min = max `created_at` = 04:02:31 UTC — one load, nothing touched since). All as expected.
- **Idempotency on hosted, twice:** first session T = `2026-08-27 04:17:07 UTC`, re-run `INSERT 0 0` ×3, `updated_at > T` 0, `created_at > T` 0. Second session T₂ = `2026-08-27 17:40:39 UTC`, same transport, same result — `INSERT 0 0` ×3, `updated_at > T₂` **0**, `created_at > T₂` **0**, totals unchanged at 19 | 3 | 1.
- **Hosted smoke through the app's read path, twice** (throwaway `.mts` run with `npx tsx`, deleted afterwards; it read only the two `NEXT_PUBLIC_SUPABASE_*` values from `.env` and asserted the project URL and the `sb_publishable_` prefix; counts printed, never a raw response): `createAnonClient()` → `listSection("experience")` **10 rows** — guardian, break-through-tech, fordham-research-assistant, wcs-data-analyst (the weighted four, in weight order), then by recency fordham-ta-cisc-2500, wcs-field-researcher, fordham-instep, fordham-step-tutor, fordham-cstep-scholar, bpl-stocker; featured 2, in_progress 1, null summaries 0 · `getFacetCounts("experience")` → all 10, **corporate 1, research 4, volunteer 1, classroom 3, coursework 1**, unfaceted 0 (per-section counts: experience rows only). Every row passed the Zod boundary — no `ContentValidationError`. This is the first non-empty read from the hosted project; the second session's run returned the same ten slugs in the same order and the same counts.
- **Handoff annotations (second session):** `handoff/feat-spine-linkedin-import.md` — all twelve open questions struck through with the decision each received in PR #15; the hosted-load and `sort_weight`/`featured` Deferred lines struck. `handoff/chore-spine-s3-closeout.md` — the seed-edit and hosted-load Deferred lines and open question 1 struck. `handoff/chore-spine-s4-closeout.md` — the seed/hosted Deferred line and open question 1 struck; the "empty until loaded" clause in Next annotated. `handoff/feat-spine-api.md` — its Deferred seed/hosted line and open question 3 struck (same class of note, outside the task's three named files, included so no handoff still says hosted is empty).

## Deviated from plan
- None against the plan. Against the task text: (1) the push went by sub-branch + auto-merge PR rather than directly, per the task's own fallback; (2) the load transport (container `psql`, pooler host, stdin) is not the literal `psql "$DATABASE_URL" -f supabase/seed.content.sql` in CLAUDE.md — same credential, same statements, same server; (3) the first smoke attempt failed before any network call (`tsx` loaded a `.ts` file as CommonJS and rejected top-level `await`) and was rerun as `.mts`; (4) the handoff lands in a second PR because PR #15 auto-merged within a minute, before the handoff existed; (5) **the chore spanned two sessions** — the first ended after the load and smoke with this handoff drafted but uncommitted and the annotations in the other handoffs not yet applied. The second session was issued the task's original preconditions (on `lane/spine` at `832228e`, hosted empty), found them already satisfied by the first session (branch `chore/spine-hosted-seed` at `14be8b5` = `origin/lane/spine`, hosted at 19 rows), and resumed rather than stopping: it re-ran the full count check read-only, repeated the idempotency re-apply and the smoke (numbers above), corrected this file, applied the annotations, committed, and opened the PR. Nothing on hosted was hand-edited; nothing local was reset, stashed, or discarded.

## Deferred
- ~~**Local `lane/spine` is now diverged**, not behind: it holds `832228e` while origin holds the squash `14be8b5` (same tree). `git pull --ff-only` there will refuse. Not touched here (no reset by the task's guard); the next lane session should point it at `origin/lane/spine` before branching.~~ — **done 2026-08-27 (feat/spine-routes):** reset to origin per the owner; see open question 2 below. The merged local branch `chore/spine-seed-content` was left in place (still present; owner's call — `handoff/chore-spine-s5-closeout.md`).
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — evaluate later; affects brief §4 and the S4 Zod enum (`FACETS` in `lib/content/schema.ts`). Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth.

## Open questions for owner
1. **Make the documented load command true on this machine**, one of: install `libpq` (`brew install libpq`, then `psql` on PATH — the direct IPv6 host works from the Mac), or amend the CLAUDE.md Database bullet to name the pooler/container transport used here (twice now). Not changed in this chore (CLAUDE.md is outside its scope).
2. ~~Local `lane/spine` divergence above — say the word and the next session resets it to `origin/lane/spine`, or do it yourself.~~ — **done 2026-08-27 (feat/spine-routes, owner's plan-mode decision):** reset to `origin/lane/spine` (`7dd47f1`, same tree); fast-forwarded to the S5 squash `d3890d4` by `chore/spine-s5-closeout`. No longer diverged.

## Next
S5 — `feat/spine-routes` on `lane/spine`: the shared route table both renderers bind to, written against the S4 contract (`listSection` per section, `getEntryBySlug` per entry URL, `getFacetCounts` for chips, `listTrophies` for the trophy case). ~~Not started.~~ — **done 2026-08-27, PR #17, squash `d3890d4`** (`handoff/feat-spine-routes.md`; merged head verified by `chore/spine-s5-closeout`). Its inputs have changed since the S4 close-out: the hosted project now holds the 19 real rows (10 / 2 / 2 / 5), so a route that prerenders in CI against hosted renders content, not an empty page; the inherited 3600 s ISR window still stands. Start by pulling `origin/lane/spine` (the local branch is diverged, above) and branching from it.
