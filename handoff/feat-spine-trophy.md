# Handoff — feat/spine-trophy

**Merged into:** `lane/spine` — PR opened with auto-merge (squash) enabled; the squash commit, merge time and CI outcome are recorded by the S8 close-out, not here.
**Plan row:** BUILD_PLAN §4, S8 — "Trophy case rendering `status` as locked / in_progress / unlocked". Session block: `PROMPTS.md` → `## Session — S8`. Its one open reading — the `status` → trophy-state mapping — plus S7's first two open questions were **put to the owner at the top of this session and answered**; all four answers are recorded below and nothing was guessed. This is the last spine sub-branch; the next step is the gate, not another `feat/`.

## Shipped
- **The four settled decisions, in the owner's words.** (1) **`archived` renders as its own state, "Archived"** — three statuses, three states, three drawings, one to one. This *rejects* `supabase/seed.sql`'s fixture comment ("in_progress and archived certifications so all three trophy states exist", written on `archived` → locked): an archived credential is one you earned and that lapsed, not one you never achieved, and calling it locked would tell a visitor something untrue. (2) **The `A Select · B Back` hint row stays on `/[section]` and `/[section]/[slug]`** — S7 open question 1, closed, no code change. (3) **Escape keeps `router.push(parentHref)`** rather than `router.back()` — S7 open question 2, closed, no code change. (4) **`/certifications` only** — the certification entry page and `/resume` keep their S6 render; both are in Deferred.
- **`app/(explorer)/trophy.tsx` — the mapping and the marker.** `TROPHY_LABELS` is `Readonly<Record<Status, string>>`, so coverage is a **`tsc` error rather than a runtime throw**: adding a fourth status to the schema fails the typecheck here until someone decides what it looks like. `Status` is imported **type-only**, so nothing new reaches any bundle. `<TrophyState>` renders a **`<span>`, never a `<p>`** — the call site wraps it in the meta line's existing `<p>`, and a `<p>` inside a `<p>` is invalid HTML that browsers silently reparse into two siblings. The label is a **sibling text node** of the glyph, not a `title` or an `aria-label` on it: the glyph is `aria-hidden`, so the state is carried by real text and survives images-off, forced colours and both themes (WCAG 1.4.1, brief §2.2's "never colour alone").
- **`TROPHY_ICONS` in `(explorer)/tiles.ts`**, keyed by the status itself so `trophy.tsx` needs no second lookup table, inlined through the existing `inlineIcon` recipe and **literal** paths. `archived` is drawn by `trophy-locked.svg` — the same kind of mismatch the file already documents for `now` → `tile-news.svg`: the *file* was named for brief §5's word, the *state* is named for the data. `trophy-unlocked.svg` is the one icon in the set carrying a literal colour, in DESIGN.md's sanctioned `var(--color-primary, #E8B11B)` form, because a fill appears only for the unlocked item — and it sits inside the path's own 3 px `currentColor` stroke, so **DESIGN.md's outline rule is satisfied by the drawing rather than by a wrapper**.
- **The row is the S6 row plus a state — `app/[section]/page.tsx`, one branch.** The condition became `section.trophyCase && entry.kind === "certification"` (the `kind` check is what narrows `metadata.category`; `trophyCase` is what says why we are drawing a trophy). Title link, subtitle, `<EntryDates>`, summary and the `<li>`'s `data-status` are untouched. That is the point rather than a convenience: the recruiter index and the trophy case stay **one markup**, not two renderings to keep in sync, and S8 adds no route, no query, no migration and **no client island**.
- **Presentation is three rules in `explorer.module.css`, tokens only.** `.trophy` (inline-flex, `--space-2`), `.trophyIcon` (`--size-icon`) and its `svg` reset. No tile face, no `--radius-sm`, no `--elevation-*` — these are list rows, not tiles. No colour of their own either: the state name inherits the meta line's `--color-text-muted`, an already-audited AA pair. No `prefers-reduced-motion` query, because `tokens.css` zeroes every `--duration-*` globally and nothing here animates.
- **Verified locally on this branch, every gate:** `lint` **0 problems** · `typecheck` clean · `test` **172** (16 files; 157 → 172, the 15 new ones in `trophy.test.tsx` and the `TROPHY_ICONS` block added to `tiles.test.ts`) · `db:test` **64** (4 files, unchanged — no query, schema or seed was touched, and `loadTrophies`/`loadSection` keep their shapes, so `supabase/tests/routes.test.ts` needed no edit) · `tokens:check` ok (57 static + 19 themed tokens, 68/68 contrast pairs, 29 icons + 2 mark files) · `build` ok with the route table **identical to S6 and S7**: `/` ○, `/_not-found` ○, `/[section]` ƒ, `/[section]/[slug]` ●, `/icon.svg` ○, `/resume` ○ 1h/1y — and `.next/prerender-manifest.json` still reports `routes["/resume"].initialRevalidateSeconds === 3600`, `compute: "static"`, `resume.html` **41718 bytes**, byte-for-byte S6's and S7's numbers.
- **The file trace went 8 → 11 and was checked, not assumed.** `.next/server/app/[section]/page.js.nft.json` now carries `trophy-unlocked.svg`, `trophy-in-progress.svg` and `trophy-locked.svg` alongside S7's eight. `next.config.ts` needed **no** change: it already names `./design/assets/icons/**` for `/[section]`, which is the route that reads these at cold start inside the serverless function. Worth restating because nothing local can reveal a gap here — `next start` and Lighthouse both run from the repo root.
- **All three states proved against the local seeds, where they exist.** The hosted project has five certifications, all `unlocked`; the fixture supplies the other two states. `NEXT_PUBLIC_*` are **inlined at build**, so pointing a running server at the local database does nothing — it takes a rebuild, and the bare PostgREST sidecar needs the `/rest/v1/` prefix `supabase-js` appends, so the smoke ran behind a throwaway path-rewriting proxy (the same rewrite `supabase/tests/harness.ts` does in its `restFetch`, moved to a socket; scratchpad-only, never committed). Against that build `/certifications` renders **8 rows — 6 Unlocked, 1 In progress, 1 Archived, and 0 occurrences of "Locked"** anywhere in the document.
- **The duplicate-id payoff, measured.** The script-stripped `/certifications` document carries **10 SVG glyphs and exactly 1 `id` attribute, 1 distinct, zero duplicates** — the `<h1>` skip-link target. All 10 glyphs are `aria-hidden="true"`, with **0** `role="img"` and exactly one `<title>` (the document's, in `<head>`). This is the case `inlineIcon`'s comment was written for in S7, now actually exercised.
- **JS-disabled proof, with S6's replacement recipe** (fetch the served HTML, strip every `<script>`, insert a `<base href>`, render the `file://` copy in headless Chrome — **not** the stale `--blink-settings=scriptEnabled=false` flag, which emits zero bytes on this Chrome). Scripts stripped: `/certifications` **10 → 0**, `/` 10 → 0, `/experience` 10 → 0. With no JavaScript the trophy case renders **12 links, 1 `<h1>`, 10 glyphs, 0 `tabindex`, 0 `<script>`, and all three state labels as text**. Both themes checked by screenshot: the unlocked trophy is saffron-filled inside its ink stroke, in-progress and archived are outlines, and the state name reads in both.
- **Lighthouse (`lhci autorun`, 3 runs × **5** URLs — `/certifications` added this session): accessibility 1.00 on every one**, best-practices 1.00, SEO 1.00, performance 0.97–1.00. **Script budget: 140,874 B (`/`, `/resume`) and 141,918 B (`/certifications`, `/experience`, the entry page), against the 250,000 B cap — 108,082 B of headroom.** Totals 253,304–261,994 B against 500,000 B. The only assertion warning is the same LCP the last three sub-branches recorded, present on unchanged URLs too; **no assertion errors**. Numbers read from the newest 15 report files only — `.lighthouseci/reports/` is never cleared.
- **Route smoke, on this branch's build:** `/` `/experience` `/experience?facet=research` `/experience/guardian` `/experience/break-through-tech` `/projects/airbnb-superhost-classifier` `/certifications` `/certifications/machine-learning-foundations` `/education` `/hobbies` `/now` `/resume` all **200** · `?facet=bogus` on both `/experience` and `/certifications`, `/experience/no-such-slug`, `/nope` all **404** · `/projects/break-through-tech` **308 → `/experience/break-through-tech`** and `/experience/machine-learning-foundations` **308 → `/certifications/machine-learning-foundations`**. **The in-progress and archived entry pages both 200** — a row that is not unlocked still links, and its entry page is real content.
- **Brief §4.1, the spine acceptance item, walked end to end.** One canonical BTT URL, reached three ways with zero duplication: `/experience` carries its own row, `/projects` reaches it through `airbnb-superhost-classifier` (`part_of`), and the trophy case reaches it through `machine-learning-foundations` (`certifies`). `/experience/break-through-tech` appears **once** on `/experience` and once on `/resume`, and **zero** times on `/projects` and `/certifications` — the record is not copied anywhere. Its entry page shows both incoming edges ("Certified by Machine Learning Foundations", "Includes Airbnb Superhost Classifier"), and `machine-learning-foundations` appears **once** in the trophy case, `unlocked`, reading "Unlocked", with its `Certifies → AI/ML Cornell Tech Fellow` edge intact.
- **IP and secret sweep over all 7 files in the change set:** `nintendo` and `joy-?con` — the two `PROHIBITED_TERMS` regexes read out of `design/tokens/build.mjs` rather than typed — **0 matches**, and **0** occurrences of `switch` in any touched file. The only repo-wide hits are the guard definitions themselves (`build.mjs`'s regex list, `scripts/linkedin/import.mjs`'s `PROHIBITED`, and its test), plus the authority documents that name the prohibition. A grep of the served HTML for all three terms returns **0** on `/`, `/certifications`, `/experience`, `/experience/break-through-tech` and `/resume`. No binaries, no third-party asset, no new dependency (`package.json` and `package-lock.json` untouched), no new `"use client"` — still S7's two islands and no more.

## Deviated from plan
- **Brief §5 names the third trophy state "locked"; this ships it as "Archived".** The owner's call, taken before any code was written, and the only deviation of substance in S8 — one word, on the one state the brief and the schema disagree about. The hollow cup still draws it, so the visual grammar brief §5 asks for is intact; what changed is that the row does not claim a lapsed credential was never earned. `trophy.test.tsx` pins it (`archived` is "Archived", and no state is "Locked") so a future edit has to face the decision rather than drift into it. **`supabase/seed.sql`'s fixture comment is now stale on this point** and was deliberately left alone — a migration-adjacent file is not S8's to edit for a comment, and the authority is this handoff plus the test.
- **`lighthouserc.json` gained a fifth URL, `/certifications`.** The S8 row does not name that file, but the one page S8 changes was not audited, and "Lighthouse a11y 1.0" is an acceptance item rather than a nicety. Run locally first to check it could hold the budget before committing: it scores a11y 1.00, performance 0.97–0.98, and the same LCP *warning* every other URL produces. Cost is roughly a fifth more time in the `lighthouse` CI job.
- **`app/[section]/page.tsx`'s trophy branch now keys on `section.trophyCase`, not on `kind` alone.** Behaviourally identical today — `/certifications` is the only section listing `certification` — but it states the intent the S8 scope words ("`/certifications` (`section.trophyCase`) as the trophy case") rather than leaving a reader to infer it from the kind.
- **No client island, no `keys.ts` addition, and no `next.config.ts` change.** All three were budgeted for in the S7 rules the session prompt carried forward; none turned out to be needed. Recorded because their absence is the design, not an omission.

## Deferred
- **Adding a class to `explorer.module.css` costs client bytes even when only a server component uses it.** `tile-row.tsx` is a `"use client"` island and imports that stylesheet, so the CSS-module **class-name map** ships in the browser bundle. S8's `.trophy` and `.trophyIcon` are the whole of its **+15 B** script delta over S7 (140,859 → 140,874 B and 141,903 → 141,918 B), traced to the class names appearing in `.next/static/chunks/`. The *drawings* do not ship — a grep of the bundle for the trophy path geometry is empty. Worth knowing before `lane/console-shell` grows that file substantially.
- **The trophy state on the certification entry page** (`/certifications/<slug>`) — the detail view shows no state today. Owner-scoped out of S8; it belongs to `feat/shell-detail-panel`, or to `feat/shell-trophy-full` if the full case takes the detail with it.
- **The trophy state on `/resume`'s "Certifications & awards"** — deliberately not added. `/resume` is an audited, prerendered `○` route and brief §2.2 asks it to be the plain-HTML document; S7 left it at the S6 baseline for the same reason. If it is ever wanted, it is a `feat/recruiter-resume-print` decision, not a shell one.
- **The full trophy view, the meta-trophy, and everything else brief §5 lists** — `feat/shell-trophy-full`, `lane/reactions`' stretch item, and the other named `lane/console-shell` sub-branches (plan §5): boot sequence and profile select, All Software grid and search, facets, detail panel, play activity, album/news, settings and notifications, gamepad, mobile shell. No filter beyond the existing facet chips ships here.
- **`media`: `<figure><img alt={alt_text}><figcaption>` from the public bucket, once one exists → `feat/admin-media`** (Decision 3). URL shape `…/storage/v1/object/public/<bucket>/<path>`. S6 deliberately ships no media markup, no bucket constant, no URL helper, and guesses no bucket name — no bucket exists today and no content row has media, so nothing could be built. The fixture's media rows correctly render as nothing.
- **The `--blink-settings=scriptEnabled=false` recipe is stale** for the installed Chrome: with that flag, `--dump-dom` and `--screenshot` both emit **zero bytes**, in old and new headless. The replacement — fetch the served HTML, strip every `<script>` tag, insert `<base href>`, render the `file://` copy — is stricter, because the scripts are absent rather than merely inert. Used again this session.
- **React 19 serialises the `dateTime` prop verbatim**, so the bytes say `<time dateTime="2026-05">`. HTML attribute names are case-insensitive, and a browser parses it to `datetime`, so it is valid and machine-readable — but a byte-level grep for `datetime=` will miss it.
- **A squash-merged branch is never an ancestor of its lane.** `git merge-base --is-ancestor` is the wrong tool for "has this branch landed?"; match the branch to its merged PR by `headRefName` and test *that PR's* `mergeCommit.oid` instead. Also note `git branch -vv` shows `[gone]` only after `git fetch --prune`, and `fetch.prune` is unset here.
- **`npm ci` reports `@emnapi/runtime` and `@img/sharp-wasm32` as `extraneous` even after a real `npm ci`** — they are lockfile-present optional platform transitives, not drift and not a dry-run artefact. And `handoff/chore-spine-s6-closeout.md` names nine `npm audit` roots where there are ten: it omits `@lhci/cli` itself. The total of 10 is correct.
- **`.lighthouseci/reports/` is never cleared** — filter by the filename timestamp before quoting a script-budget delta, or you will compare across generations. **`puppeteer-core` is already available** via the `@lhci/cli` dev tree for real keyboard proofs, but the driver script must live **inside the repo** (Node resolves `node_modules` upward from the script's own directory). **`readFileSync` in a per-request route needs `outputFileTracingIncludes`** — no local run reveals the gap, because `next start` and Lighthouse both run from the repo root.
- **`NEXT_PUBLIC_*` are inlined at build, not read at runtime**, so a smoke against different data needs a rebuild, not an env override on `next start`. And the local PostgREST sidecar is bare — no `/rest/v1` prefix — so the app's own client cannot reach it without the rewrite `supabase/tests/harness.ts` performs in `restFetch`.
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- Unchanged from `handoff/feat-spine-routes.md`: `alternates.canonical` / sitemap / OG / structured data → `feat/recruiter-seo` (needs `metadataBase` = the domain, plan §8) · `app/not-found.tsx` and error boundaries → Phase 3 · facet as a path segment only if `/[section]` must be static. Its "trophy states → S8" line is now discharged.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — evaluate later; affects brief §4 and the S4 Zod enum (`FACETS` in `lib/content/schema.ts`). Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth. **S8 feels this directly:** the trophy state is read straight off `status`, so whichever field wins that argument decides what the trophy case says.

## Open questions for owner
1. **Resume order is tile order, not a strict date sort** — `compareRecency` (featured, then `sort_weight`, then most recent start). That is the same order the section pages use, so the two never disagree, but it means a featured or heavily weighted row can sit above a more recent one. Confirm, or say the resume should sort strictly by date. Unchanged from S6 and S7.
2. **The `<dl>` keys on the entry page** — `cause` (experience), `activities` (education), `credential_id` (certification); `location` renders in the dates line and `credential_url` is not rendered because the `links` row of kind `profile` already is. Strike any of these if unwanted. Unchanged from S6 and S7.
3. **`psql` / libpq is still not installed on this machine**, so the hosted-seed chore in `CLAUDE.md`'s Database bullet cannot be run as written. Unchanged from S3 onward, and it blocks nothing today: the hosted content seed is already loaded and no seed edit has landed since.
4. **The dev server on port 3000 is still the owner's to restart** — `npm run dev`. This session used 3001 and 3002 for the smoke and released both; `lhci` used 3000 and released it too. The compose containers were found running and were left running.
5. **Vercel Protection Bypass secret** — still unset, so preview URLs stay SSO-gated and cannot be curled; "preview live" continues to mean deployment success. Unchanged.
6. **The domain is registered and nothing in the spine consumes it.** `kerwynjean.dev` (Porkbun), canonical origin `https://kerwynjean.dev` — scheme, no trailing slash, no `www`. It lands in the repo as `metadataBase` in `feat/recruiter-seo` (Phase 1, `lane/recruiter`), never in a spine sub-branch. Still the owner's to do: attach the domain in Vercel, point DNS (nameservers, or the records Vercel supplies — DNS may stay at Porkbun), enable auto-renew and 2FA. **Do not** create Porkbun's Let's Encrypt certificate; Vercel issues and renews its own. `.dev` is HSTS-preloaded at the TLD, so a hardcoded `http://` reference to that host hard-fails rather than redirecting, and a lapsed registration means connection errors rather than a parked page.
7. **A second interactive session's uncommitted edit is still in the working tree.** `PROMPTS.md` carries a **whitespace-only** diff (one blank line removed at the top, one added before the spine-gate bullet list) made by session `personal-website-75` during S7, against CLAUDE.md's one-agent-per-lane rule. S7 kept it out of its commit by staging explicit paths and S8 did the same. It is still uncommitted — commit or discard it separately.

## Next
**The spine gate — `lane/spine` → `main`, BUILD_PLAN §7, the only human merge gate and always the owner's call.** S8 is the last sub-branch; there is no S9. A close-out chore verifies the merged lane head first-hand before the owner opens that PR. Its §4 acceptance items are now all supplied: one BTT record in Experience, Projects and the trophy case with zero duplication (walked above) · the same URL in both renderers (S7's no-mode-switch decision) · recruiter path with JavaScript disabled (S7, re-proved here on `/certifications`) · keyboard through the tile row to a detail page with no trap (S7) · green CI and a live preview. What changes the moment it merges is written down in `PROMPTS.md` under "After S8 — the spine gate" — the keep-warm schedule starts, Dependabot begins, the Supabase GitHub integration applies migrations to production, and Vercel deploys `main` to Production for the first time; read it there rather than rediscovering it. After the gate, Phase 1 Pair 1 (`lane/recruiter` + `lane/ingestion`), now blocked only on Steam profile visibility since the domain is settled, with plan §9's items verified at build time rather than from memory.
