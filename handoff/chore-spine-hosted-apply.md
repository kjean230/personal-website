# Handoff — chore/spine-hosted-apply

**Merged into:** `lane/spine`
**Plan row:** none — a chore closing the S2 (BUILD_PLAN §4) hosted-Supabase loose ends before S3. No plan row, no app code, no schema change.

## Shipped
- **Hosted schema go-live (2026-08-25).** From the repo root, with `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` exported from the git-ignored `.env` for that command only: `npx supabase@latest db push --dry-run` listed exactly `20260825010000_spine_schema.sql` and `20260825010100_spine_rls.sql` with `seeds: []`; `npx supabase@latest db push --yes` applied them; `supabase migration list --linked` shows both, local = remote. Target: project `zfsslawwrgyfkupawrnm` ("kjean230's personal website", us-west-2, Free plan, Postgres 17.6.1.155, ACTIVE_HEALTHY).
- **Hosted verification (read-only, as `postgres` over the direct connection):** `server_version` 17.6 · all seven tables present with `relrowsecurity = true` · 15 policies (2 per content table, 3 on `reactions`) · `reaction_counts` view, `is_admin()`, `reaction_emoji_allowlist()`, `set_updated_at()` present · `supabase_migrations.schema_migrations` holds the two versions · **0 rows** in `entries`/`tags`/`reactions` (no fixture data) · anon grants are `SELECT` on the six content tables and the view, and column-level `INSERT (emoji, entry_id, ip_hash)` on `reactions` only · under an anon session: `is_admin()` = false, content select allowed, `select … from reactions` → `42501`, insert into `entries` → `42501`, a reaction with a raw IP → `23514`; with the `app_metadata.role = 'admin'` claim `is_admin()` = true.
- **Hosted `db:test` run once**, as instructed, because the fixture is absent: it stopped in `beforeAll` ("no fixture rows"), 31 tests skipped, nothing written. Hosted RLS verification is therefore limited to the manual probes above; the full suite runs on every PR against a fresh database in CI.
- **Working-tree hygiene:** `lane/spine` was clean; `app/page.tsx`, `package.json`, `package-lock.json` unchanged from 11963f6; no `utils/supabase/`, no `middleware.ts`/`proxy.ts`. Nothing to revert.
- **Secret containment:** `.env` is ignored (`.gitignore:242`, `.env*`). `git grep` over `origin/lane/spine` and a filesystem grep that opened every file outside `node_modules`/`.next`/`.git` (paths only, `/usr/bin/grep -rIl`) found the credential *names* in prose in `handoff/feat-spine-schema.md` (no `=` assignments — not a credential) and the ignored `supabase/.temp/pooler-url` (no password embedded). No secret value exists in any tracked or unignored file. The hosted `DATABASE_URL` is present in `.env` and was used, unread, via the environment.
- **Docs:** `handoff/feat-spine-schema.md` annotated (hosted apply done; project and branching questions resolved); CLAUDE.md Database bullet gains the hosted-apply convention and the Free-plan decision.
- Memory for future sessions: never print a raw provider-API response (see "Open questions" 1).

## Deviated from plan
- None against the plan. Against this chore's task list: **step 4 (Vercel env vars) was not done** — the Vercel CLI is logged out on this machine and the repo is not `vercel link`ed; the task said to stop and ask rather than guess, so it is handed back below.
- The Management API `/postgrest` read in step 6 was printed unfiltered and it included the project's `jwt_secret` — a guard breach on my side, reported in "Open questions" 1 with the fix.

## Deferred
- Vercel `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` for Production + Preview — blocked on `vercel login` (+ `vercel link` to `kjean/personal-website`); exact commands in "Open questions" 2. Nothing in S3 needs them; S4 does.
- Hosted **auth config drift** from `supabase/config.toml` (report only, per step 6): hosted has `disable_signup = false`, `password_min_length = 6`, `site_url = http://localhost:3000`, email confirmations off; `config.toml` says signups off, 12 chars, confirmations on. Branching is off, so `config.toml` never reaches the hosted project by itself — `npx supabase@latest config push` would align it. RLS does not depend on it (`is_admin()` needs the `app_metadata` claim), so this is posture, not exposure. Owner's call; `feat/admin-auth` is the natural place.
- A push-on-merge path for future migrations (Actions workflow with `SUPABASE_ACCESS_TOKEN`/`SUPABASE_DB_PASSWORD`/`SUPABASE_PROJECT_ID` secrets) — or rely on the Supabase GitHub integration's deploy-to-production, which applies `supabase/migrations` on merge to `main`. Decide at the lane → `main` gate.

## Open questions for owner
1. **Rotate the JWT secret.** While reading the Data API settings via the Management API I printed the full `/postgrest` response, which includes `jwt_secret`; it is now in this session's transcript on disk. Please rotate it (Dashboard → Project Settings → JWT Keys / API → generate a new secret). Impact is minimal: the project has no users yet, the app uses the `sb_publishable_…` key (independent of the JWT secret), and the only casualties are the legacy `anon`/`service_role` JWT keys, which nothing uses.
2. **Vercel env vars (step 4).** Run `npx vercel@latest login`, then `npx vercel@latest link --yes --scope kjean --project personal-website`, then either tell me to finish step 4, or do it yourself: `npx vercel@latest env add NEXT_PUBLIC_SUPABASE_URL production` / `… preview` and the same for `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (values: `https://zfsslawwrgyfkupawrnm.supabase.co` and the `sb_publishable_…` key from `.env` — both public). Add nothing else.
3. **Keep-warm.** The project was created 2026-08-24 and has no traffic; Free projects pause after ~7 days of low database activity. Until the ingestion cron (Pair 1) or the deployed site queries it, expect a pause (one-click restore). A trivial daily read in a scheduled Actions job would prevent it if that matters before then.
4. Proposed wording for BUILD_PLAN §7 Ops (not edited — owner's file): replace "Green CI, preview deploy live on an isolated database branch" with "Green CI including the database job (migrations + RLS tests on a fresh database per run); preview deploy live. Supabase Branching is not used (Free plan); previews share the hosted project." The matching brief §9 line ("Preview deploy per PR with an isolated database branch") would read "Preview deploy per PR; database isolation is the CI database job."

## Next
S3 — `feat/spine-linkedin-import` on `lane/spine`. Not started; the LinkedIn export was not touched in this session.
