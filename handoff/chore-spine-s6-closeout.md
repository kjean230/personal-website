# Handoff — chore/spine-s6-closeout

**Merged into:** `lane/spine`
**Plan row:** none — the S6 close-out: verify that PR #21 merged and that the merged lane head is green first-hand, smoke the merged routes, annotate the handoffs, and act on the owner's two housekeeping calls. Documentation and verification only: no app code, no `lib/`, no `app/`, no schema change, no migration, no seed change, no dependencies.

## Shipped
- **PR #21 (`feat/spine-recruiter` → `lane/spine`) merged** by auto-merge as squash **`09f8817`** at **2026-08-28 16:19:44 UTC**. Every check green: Lint, Typecheck, Test, Design tokens check, Build, Lighthouse budget, Database migrations & RLS, **`CI green`**, Vercel Preview Comments; Supabase Preview skipped (Branching unused, settled). `origin/feat/spine-recruiter` was deleted by the merge. `git diff fcd81af origin/lane/spine` is empty — the squash carries the identical tree — and `09f8817` is an ancestor of the lane head. This branch was cut from `origin/lane/spine` and tracks it.
- **Merged lane head verified first-hand at `09f8817`, every number matching the S6 baseline:** `lint` **0 problems** · `typecheck` clean · `test` **124** (12 files) · `tokens:check` ok (57 static + 19 themed tokens, 68/68 contrast pairs, 29 icons + 2 mark files) · `db:reset && db:test` **64** (4 files: 31 RLS + 9 content + 15 API + 9 routes) against the local Docker Postgres + PostgREST sidecar · `build` ok — `/` ○, `/_not-found` ○, `/[section]` **ƒ**, `/[section]/[slug]` **●**, `/icon.svg` ○, `/resume` **○ with a `1h` revalidate / `1y` expire column**, and `.next/prerender-manifest.json` → `routes["/resume"].initialRevalidateSeconds === 3600`, `compute: "static"`, `resume.html` **41718 bytes** · `npm audit` **10** `{low 2, moderate 1, high 7}`, every advisory rooted in the `@lhci/cli` dev tree (`uuid`, `tmp`, `puppeteer-core`, `lighthouse`, `extract-zip`, `inquirer`, `external-editor`, `@puppeteer/browsers`, `@lhci/utils`) — **0 in runtime dependencies, 0 new**. `npm ci` was deliberately not run (see Deviated).
- **Smoke of the merged routes — locally, on the squash tree** (`npm run build && npx next start -p 3001`; Next loaded the git-ignored `.env` itself, never sourced or printed; status codes and element counts only, never a row): `/` `/experience` `/experience?facet=research` `/experience/guardian` `/experience/break-through-tech` `/projects/airbnb-superhost-classifier` `/certifications/machine-learning-foundations` `/education` `/hobbies` `/now` `/resume` all **200** · `?facet=bogus`, `/experience/no-such-slug`, `/nope` all **404** · `/projects/break-through-tech` **308 → `/experience/break-through-tech`**. Counts identical to S6's: `/resume` **4 `<h2>`, 19 `<h3>`, 19 `<article>`, 32 `<time>`, 1 external link** · `/experience` **10** entry links and **5** facet chips, `?facet=research` **4** · the certification page exactly **1** `rel="noopener"` and **0** `target=` · the header's Resume link on every page (2 on `/` = header + home nav) · **0** error lines in the server log. Server stopped afterwards; port 3001 released.
- **JS-disabled proof re-run with S6's replacement recipe, not the stale flag:** `/resume` **12** `<script>` tags stripped → **0** remaining, entry page **10 → 0**; a `<base href>` inserted and the `file://` copies rendered headless. `/resume` renders **1 `<h1>` · 4 `<h2>` · 19 `<h3>` · 32 `<time>` · 0 `<script>` · 23 links · 3518 chars of visible text**; `/experience/break-through-tech` **1 `<h1>` · 1 `<h2>` · 1 `<time>` · 0 `<script>` · 6 links · 895 chars**. `grep -rn "use client" app lib` is **empty**. Byte-level `dateTime=` **32** and `datetime=` **0** on `/resume`, confirming the React 19 serialisation note first-hand.
- **Secret / PII / prohibited-term sweep over the merged head, in two passes** — the **22** files PR #21 touched, and all **131** tracked files (no tracked file falls under `node_modules`, `.next`, `.git`, `.vercel`, `supabase/.temp`, `supabase/.branches`, and **no `.env*` file is tracked**): `gmail`, `@glic`, `@fordham.edu`, `linkedin.com/in/` — only the term-list lines of `PROMPTS.md` and the S3/S4/S6 handoffs, never an address · the secret-key and legacy-JWT prefixes (read out of `handoff/chore-spine-s4-closeout.md`, not typed) — only that line; a structural grep for JWT-shaped literals returned **no matches** · `service_role` — role *name* only across 8 files (the RLS migration's grants ×10, `rls.test.ts`'s type union, `docker-compose.yml`'s comment, and four docs) · **the hosted `DATABASE_URL` password** (parsed in-process with `urllib.parse`, compared as a fixed string, never printed, never sourced) — **0 matches in both passes** · both `PROHIBITED_TERMS` regexes loaded programmatically out of `design/tokens/build.mjs` — only the constraint text (`BUILD_BRIEF.md`, `BUILD_PLAN.md`, `CLAUDE.md`, `PROMPTS.md`), its enforcement (`build.mjs`, `scripts/linkedin/`), and prior sweep reports. **Zero occurrences in UI copy, page metadata, filenames, alt text, seed content, or `lib/`.** Verdict: clean.
- **The owner's two housekeeping calls, taken.** (1) **The 15 merged local branches were deleted** — nine `chore/*` (prompts-spine-s6, prompts-spine-s6-reconcile, hosted-apply, hosted-config, hosted-seed, seed-content, s3/s4/s5-closeout) and six `feat/spine-*` (ci, schema, linkedin-import, api, routes, recruiter). Each was verified landed **before** deletion, and the naive test is wrong: `git merge-base --is-ancestor <branch> origin/lane/spine` answers **no** for all 15, because every one was **squash**-merged and a squash tip is never an ancestor of the lane. The test used instead matches each branch against `gh pr list --state merged` by `headRefName` and confirms *that PR's* `mergeCommit.oid` is an ancestor of `origin/lane/spine` — all 15 passed. Tips recorded in this session before deletion and still in the reflog. Kept: `feat/identity-proposals`, `lane/identity`, `lane/spine`, `main`, and this branch. (2) **`PROMPTS.md`** gained a bracketed correction beside `--blink-settings=scriptEnabled=false` in the already-run `## Session — S6` block — the only live place the stale flag was written down; the S7 and S8 blocks never carried it, and nothing else in that historical block was touched.
- **Handoff annotations:** `handoff/feat-spine-recruiter.md` — the squash commit, merge time and full CI outcome recorded under "Merged into"; the stale-Chrome Deferred bullet struck as corrected; open question **4** (branches) struck and answered with the squash-merge verification note; open question **6** (the port-3000 `next dev`) struck as **moot** (see Deviated). Questions 1, 2, 3, 5 and 7 are untouched and carry forward. `handoff/feat-spine-routes.md` and `handoff/chore-spine-s5-closeout.md` — each `## Next` pointer at S6 struck from "Not started." to done, with the date and squash. Nothing deleted.
- **Three documentation-accuracy findings recorded, S6's own text left as written:** (1) `handoff/feat-spine-recruiter.md` says its sweep covered "**20** files"; `git diff --stat cfe1ed6 09f8817` lists **22** — 20 is reachable only by excluding the handoff itself and `package-lock.json`, which the sentence does not say. This chore's sweep covered all 22. (2) Three files PR #21 changed are absent from its `## Shipped` list — `package.json` and `package-lock.json` (implied only by the `marked@18.0.11` line under Verification) and `supabase/tests/api.test.ts` (implied only by the `listLinks` line). All three are real and correct; the list is simply incomplete. (3) **Informational, not drift:** `.next/prerender-manifest.json` also lists `/_global-error`, Next 16's auto-generated boundary — there is no `app/global-error.tsx`, it is absent from S6's recorded six-route baseline, and it does **not** appear in the `next build` route table. Recorded once so the next close-out does not re-investigate it.

## Deviated from plan
- **`npm ci` was deliberately not run.** It does `rm -rf node_modules`, which would have destroyed the owner's running `next dev` on port 3000 — the process the owner had, in the same session, said to leave alone. Substituted `npm ci --dry-run` (the lockfile resolves; the 80 packages it reports "adding" are Linux-only optional `sharp`/`emnapi` binaries npm skips on darwin) plus `npm ls --depth=0`, which confirms every direct dependency at its exact locked version: `marked@18.0.11`, `next@16.3.2`, `react@19.2.8`, `react-dom@19.2.8`, `zod@4.4.3`, `@supabase/supabase-js@2.112.4`. The two `extraneous` lines npm prints (`@emnapi/runtime`, `@img/sharp-wasm32`) are optional platform transitives, not lockfile drift.
- **The owner's dev server on port 3000 died anyway, and this chore caused it.** `npm run build` rewrote `.next/` underneath the running `next dev` — the same collision, reached by a path the `npm ci` decision did not guard. It was **not** killed directly: the only `kill` in this session targeted its own `next start` on 3001. Port 3000 is now free and no `next` process is running; one `npm run dev` restores it. Not restarted, because it is the owner's process to start. This is what moots open question 6.
- **The smoke ran on port 3001, not 3000** — 3000 was still occupied when the server was started. `lighthouserc.json` is untouched and still audits `localhost:3000`. Lighthouse was not re-run locally: the Lighthouse budget job passed on the merged head in CI, which is the gate.

## Deferred
- **`media`: `<figure><img alt={alt_text}><figcaption>` from the public bucket, once one exists → `feat/admin-media`** (Decision 3). URL shape `…/storage/v1/object/public/<bucket>/<path>`. S6 deliberately ships no media markup, no bucket constant, no URL helper, and guesses no bucket name — no bucket exists today and no content row has media, so nothing could be built. The fixture's media rows correctly render as nothing.
- **The `--blink-settings=scriptEnabled=false` recipe is stale** for the installed Chrome: with that flag, `--dump-dom` and `--screenshot` both emit **zero bytes**, in old and new headless. The replacement — fetch the served HTML, strip every `<script>` tag, insert `<base href>`, render the `file://` copy — is stricter, because the scripts are absent rather than merely inert. Re-confirmed first-hand this session. `PROMPTS.md` is corrected; this bullet is the carrier for any future prompt that reaches for the flag.
- **React 19 serialises the `dateTime` prop verbatim**, so the bytes say `<time dateTime="2026-05">`. HTML attribute names are case-insensitive, and a browser parses it to `datetime`, so it is valid and machine-readable — but a byte-level grep for `datetime=` will miss it. Re-confirmed: 32 vs 0 on `/resume`.
- **A squash-merged branch is never an ancestor of its lane.** `git merge-base --is-ancestor` is the wrong tool for "has this branch landed?"; match the branch to its merged PR by `headRefName` and test *that PR's* `mergeCommit.oid` instead. Also note `git branch -vv` shows `[gone]` only after `git fetch --prune`, and `fetch.prune` is unset here.
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- Unchanged from `handoff/feat-spine-routes.md`: `alternates.canonical` / sitemap / OG / structured data → `feat/recruiter-seo` (needs `metadataBase` = the domain, plan §8) · `app/not-found.tsx` and error boundaries → Phase 3 · ~~body, dates, links, media, tags, the section index proper and the resume → **S6**~~ — **done 2026-08-28 (`feat/spine-recruiter`)**, except media, which moved to `feat/admin-media` above · trophy states → **S8** · facet as a path segment only if `/[section]` must be static.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — evaluate later; affects brief §4 and the S4 Zod enum (`FACETS` in `lib/content/schema.ts`). Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth.

## Open questions for owner
1. **Resume order is tile order, not a strict date sort** — `compareRecency` (featured, then `sort_weight`, then most recent start). That is the same order the section pages use, so the two never disagree, but it means a featured or heavily weighted row can sit above a more recent one. Confirm, or say the resume should sort strictly by date. Unchanged from S6.
2. **The `<dl>` keys on the entry page** — `cause` (experience), `activities` (education), `credential_id` (certification); `location` renders in the dates line and `credential_url` is not rendered because the `links` row of kind `profile` already is. Strike any of these if unwanted. Unchanged from S6.
3. **`psql` / libpq is still not installed on this machine**, so the hosted-seed chore in `CLAUDE.md`'s Database bullet cannot be run as written. Unchanged from S3 onward, and it blocks nothing today: the hosted content seed is already loaded and no seed edit has landed since.
4. **The dev server this chore killed** — port 3000 is free and nothing is running there. Restart it with `npm run dev` if you want it back; this session deliberately did not.
5. **Vercel Protection Bypass secret** — still unset, so preview URLs stay SSO-gated and cannot be curled; "preview live" continues to mean deployment success. Unchanged.
6. **S7's two `[OWNER: …]` fields are still unfilled**, so `## Session — S7` in `PROMPTS.md` is not runnable: where shell code lives (recommended: `app/(explorer)/` holds components only, no `page.tsx` or `layout.tsx` inside the group) and how S7 selects a renderer at the same URL (recommended: no mode switch; the tile row is the enhanced rendering of the home page's existing section nav). A third bracketed choice sits in its scope: whether to show or hide tiles for sections whose count is 0.

## Next
S7 — `feat/spine-console-tile` on `lane/spine`, **blocked** until the two `[OWNER: …]` fields above are settled. It is the repo's first `"use client"` island: the script budget is 250 kB and S6 measured **139.4 kB**, so record the delta. Start by pulling `origin/lane/spine` (at this chore's squash once it merges) and branching from it. Then S8 — `feat/spine-trophy` (confirm the `status` → trophy mapping; S2's fixture was written on `archived` → locked), then the spine gate `lane/spine` → `main` with the BUILD_PLAN §7 checklist signed. ~~Not started.~~ — **S7 done 2026-08-29, PR #23, squash `d996cb7`; S8 done 2026-08-29, PR #24, squash `589c63b`; the spine gate done 2026-08-29, PR #26, squash `2c80113`** (`handoff/feat-spine-console-tile.md`, `handoff/feat-spine-trophy.md`, `handoff/chore-post-gate-closeout.md`; merged heads verified by `chore/spine-s8-closeout` and `chore/post-gate-closeout`). The **blocked** above is discharged too — S7's two `[OWNER: …]` fields were settled at the top of that session.
