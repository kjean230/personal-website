# Handoff — chore/spine-s5-closeout

**Merged into:** `lane/spine`
**Plan row:** none — the S5 close-out: verify that PR #17 merged and that the merged lane head is green first-hand, smoke the merged routes, annotate the handoffs. Documentation and verification only: no app code, no `lib/routes`, no schema change, no migration, no seed change, no dependencies.

## Shipped
- **PR #17 (`feat/spine-routes` → `lane/spine`) merged** by auto-merge as squash **`d3890d4`** at **2026-08-27 21:00:34 UTC**. Every check green: Lint, Typecheck, Test, Design tokens check, Build, **Lighthouse budget** (the one new CI risk — the job now audits `/experience`, a per-request route reading the hosted project, and `/resume` beside `/`), Database migrations & RLS, CI green, Vercel; Supabase Preview skipped (Branching unused, settled). `origin/feat/spine-routes` was deleted by the merge. `git diff 34777d3 d3890d4` is empty — the squash carries the identical tree. Local `lane/spine` (`7dd47f1`, reset to origin during S5) fast-forwarded cleanly to `d3890d4`; this branch was cut from it.
- **Merged lane head verified first-hand at `d3890d4`:** `lint` 0 problems · `typecheck` clean · `test` **90** (10 files) · `tokens:check` ok (57 static + 19 themed tokens, 68/68 contrast pairs) · `build` ok — `/` ○ static, `/resume` ○ static, `/[section]` ƒ dynamic, `/[section]/[slug]` ● SSG-on-demand, `/_not-found` ○, `/icon.svg` ○ · `db:test` **62** (4 files: 31 RLS + 9 content + 14 API + 8 routes) against the local Docker Postgres + PostgREST sidecar.
- **Smoke of the merged routes — locally, on the squash tree** (`npm run build && npm run start`; Next loaded the git-ignored `.env` itself, never sourced or printed; port 3000 confirmed free before and released after; status codes and link counts only, never a row): `/` **200** (2 Resume links: header + home nav) · `/experience` **200** (10 entry links) · `/experience?facet=research` **200** (4) · `/experience?facet=bogus` **404** · `/experience/guardian` **200** · `/projects/break-through-tech` **308 → `/experience/break-through-tech`** · `/experience/break-through-tech` **200** (2 related links) · `/experience/no-such-slug` **404** · `/nope` **404** · `/certifications` **200** (5) · `/resume` **200** · `/privacy` **404** · `/admin` **404**; every 200 page carries the header's Resume link; 0 error lines in the server log. Identical to the S5 pre-merge smoke.
- **The Vercel preview could not be smoked** (see Deviated): the squash's preview deployment (`personal-website-k9rzp5e31-kjean.vercel.app`, deployment `6130958720`, status `success` at 21:01:01 UTC) answers every URL — including `/` — with a `302` to `vercel.com/sso-api`, i.e. Vercel Deployment Protection (Vercel Authentication) is on for previews. Passing it needs a Vercel session or a Protection Bypass token, which is a credential beyond the two `NEXT_PUBLIC_*` values, so the task's local fallback was used. The deploy itself succeeded and CI's Lighthouse job (which runs its own `next start`, not the preview) is what gates the routes.
- **Handoff annotations:** `handoff/feat-spine-routes.md` — the squash commit, merge time and CI outcome recorded under "Merged into"; nothing in its Deferred or Open questions was resolved by this chore, so nothing is struck there. `handoff/chore-spine-hosted-seed.md` — open question 2 (local `lane/spine` divergence) and its matching Deferred bullet struck through as done by S5's owner-approved reset. Nothing deleted.

## Deviated from plan
- None against the plan. Against the task text: (1) the smoke ran locally rather than on the Vercel preview, because the preview is SSO-gated (above) — the task named that fallback; (2) the owner's branch-cleanup decision was not given, so the merged local branches were **left in place** (`chore/spine-hosted-seed`, `chore/spine-seed-content`, `chore/spine-s3-closeout`, `chore/spine-s4-closeout`, `chore/spine-hosted-apply`, `chore/spine-hosted-config`, and now `feat/spine-routes` at `34777d3`, whose remote is gone) — nothing deleted, nothing reset.

## Deferred
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- Unchanged from `handoff/feat-spine-routes.md`: `alternates.canonical` / sitemap / OG / structured data → `feat/recruiter-seo` (needs `metadataBase` = the domain, plan §8) · `app/not-found.tsx` and error boundaries → Phase 3 · body, dates, links, media, tags, the section index proper and the resume → **S6** · trophy states → **S8** · facet as a path segment only if `/[section]` must be static.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — evaluate later; affects brief §4 and the S4 Zod enum (`FACETS` in `lib/content/schema.ts`). Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth.

## Open questions for owner
1. **Make the documented load command true on this machine** (carried from `handoff/chore-spine-hosted-seed.md`), one of: install `libpq` (`brew install libpq`, then `psql` on PATH — the direct IPv6 host works from the Mac), or amend the CLAUDE.md Database bullet to name the pooler/container transport used twice now. Not changed here.
2. **Empty sections in the home nav** (carried from `handoff/feat-spine-routes.md`): `/hobbies` and `/now` render an empty state until `lane/content` — keep them linked from `/` so the URL contract is visible, or hide empty sections? The S7 tile row is the natural place to decide.
3. **Vercel preview protection.** Previews are SSO-gated, so "preview deploy live" (plan §7 Ops, brief §9) is true for you but not for a reviewer or a smoke script. Options: leave it (previews are for you; CI's own Lighthouse run gates the routes), or add a Protection Bypass for Automation secret to Actions so a future close-out can smoke the preview — never in the repo. Your call; nothing changed here.
4. **Merged local branches** (six chores + `feat/spine-routes`): delete or keep? Left in place pending your decision.

## Next
S6 — `feat/spine-recruiter` on `lane/spine`: SSR section index + detail page, functional with JS disabled, plain `/resume`. Bind to the route table (`lib/routes/table.ts`, `lib/routes/load.ts`): render on the page files S5 already gives you (`app/[section]/page.tsx`, `app/[section]/[slug]/page.tsx`), replace the `/resume` placeholder, keep one page file per URL and `notFound()` / `permanentRedirect()` at the top level of the page. Start by pulling `origin/lane/spine` (at this chore's squash once it merges) and branching from it. Not started.
