# Handoff — chore/prompts-spine-s6-reconcile

**Merged into:** `lane/spine`
**Plan row:** none — a documentation chore between PR #19 (`chore/prompts-spine-s6`) and S6: reconcile the committed S6 session block with owner decisions from an earlier, superseded local S6 brief that surfaced after #19 merged. No app code, no schema change, no migration, no seed change, no dependencies.

## Shipped
- **The discovery.** While an adversarial verification workflow (four finders, four skeptics, read-only) was checking the session handoff prompt against the repo, it found `handoff/S6-IMPLEMENT.md` — 22.8 KB, written 2026-08-27 19:12 by an earlier planning session, hidden from `git status` by two lines in `.git/info/exclude` (not `.gitignore`), never committed — and the plan file it declared binding, `~/.claude/plans/task-start-s6-feat-spine-recruiter-velvety-chipmunk.md` (19:10). Both predate the S6 block committed in PR #19 (`c796b07`, 20:14) and prescribed a different S6: `marked@18.0.11` by name with headings demoted; `/resume` ordered Experience · Projects · Education · Certifications & awards **with the entries' links rows**; a `listLinks` addition to `lib/content/queries.ts`; `<img>` from a guessed `media` bucket; day dates as "May 1, 2026". Its §2 recorded three of those as **owner decisions** from that session's task — decisions the #19 block had overridden on points its author (this session) had decided alone (media, `/resume` order and links, no query-layer change) without knowing they existed.
- **Owner decisions (plan mode, 2026-08-27, after the discovery):** (1) reconcile into `PROMPTS.md` rather than let either document win outright; (2) delete `handoff/S6-IMPLEMENT.md` and its two exclude lines now.
- **`PROMPTS.md` — the `## Session — S6` block amended in place** (the S7, S8 and gate sections, the Stop rule and everything above the S6 section untouched):
  - An "Amended 2026-08-27" paragraph outside the fence, and `handoff/chore-prompts-spine-s6.md` + this file added to the read-first list.
  - **Entry page** paragraph: dates line with `location`, body via `renderMarkdown` only when non-null, a `<dl>` of the typed keys (`location`, `cause`, `activities`, `credential_id`), a Links list with `rel="noopener"` and no `target`, Tags grouped by category, Related as is; the credential link is the `links` row of kind `profile` and `metadata.credential_url` is not rendered separately; media nothing.
  - **Dates** paragraph: `lib/render/dates.ts`, no `Date` objects, day → "May 1, 2026", absent precision = full date, "never invent a day", `<time datetime>` at precision.
  - **`/resume`** paragraph: the owner's order — Experience · Projects · Education · Certifications & awards — each `<section aria-labelledby>` of `<article>` rows with title link, subtitle, dates, summary and the entry's `links` rows; nothing else; tile order; `loadResume` composed from `listSection` × 3, `listTrophies` and the new `listLinks`, links grouped by `entry_id`; `SITE_NAME` lifted into one constant.
  - **Decision 2 (markdown):** the dependency is `marked@18.0.11` exact-pinned (advisory check recorded: GHSA-6v9c-7cg6-27q7 covers 18.0.0–18.0.1 only), `npm audit` before/after (baseline 10, all `@lhci/cli`), the fallback if an advisory appears, the renderer-override boundary (overrides must return strings; `html` escaped and `<p>`-wrapped when block; `link` allows `^https?://` and site-relative only with `rel="noopener"`; `image` https only; heading depths normalised so the shallowest is h2 — axe `heading-order`), output p / ul / ol / li / strong / em / code / a / h2–h6, and the test list.
  - **Decision 3 (media):** the owner's rule ("`<img alt>` only when a Storage public URL can actually be built") and its consequence today — no bucket exists, so S6 ships no media markup, constant or helper and guesses no bucket name; the exact Deferred line for `feat/admin-media`.
  - **Decision 8 (query layer):** exactly one sanctioned addition, `listLinks(client) → Link[]`, with its query shape, the header-list / `RouteQueries` / `defaultQueries` edits, the eight typed `queries` literals it breaks and their fixes, the `api.test.ts` assertions, and `db:test` 62 → 64; anything else remains a stop-and-report contract change.
  - New **"Verified at plan time (2026-08-27) — reuse, do not re-derive"** paragraph carrying the earlier pass's findings that change what S6 does: Next 16.3.2 prerender/ISR mechanics for `/resume` (glyph stays ○, `initialRevalidateSeconds: 3600` in the manifest, a thrown prerender error fails the build, what would make it dynamic); the Lighthouse/axe weighted rules the markup touches (`heading-order`, `definition-list`/`dlitem`, `list`/`listitem`, `link-in-text-block` → keep underlines, `target-size` → `min-height: var(--size-touch-min)` on standalone chips); `npm test` does not typecheck; the docs that become false (CLAUDE.md sentence, `lib/routes/table.ts` header row — comment only, `app/resume/page.tsx` comment); `no-img-element` moot; no profile row; 18 month / 1 day precision.
  - **Verification** list: exact-total reporting for `npm test`, the `1h` revalidate column / manifest check for `/resume`, `db:test` 64, Docker Desktop prerequisite; smoke adds the credential-link count and `/resume` = four `<h2>` + 19 `<h3>` + the name in `<h1>`; stop the server after.
  - **Then** paragraph: three-commit recipe, `git push -u origin feat/spine-recruiter` (the branch tracks `origin/lane/spine`; never a bare `git push`, never `HEAD:lane/spine`), `gh pr create --base lane/spine --body-file` + `gh pr merge --auto --squash`, the PR #17 / #19 body skeleton with the dependency and Zod ticks spelled out, two new Open questions for the S6 handoff (resume order = tile order — confirm; the `<dl>` keys — strike if unwanted), and the CLAUDE.md updates including the now-false "`next build` needs no Supabase variables".
- **`handoff/chore-prompts-spine-s6.md`** — its "Owner decisions recorded in the S6 block" bullet annotated with the amendment and a pointer here. Nothing struck, nothing deleted.
- **Deleted locally, per the owner:** `handoff/S6-IMPLEMENT.md` and the two `S6-IMPLEMENT` lines in `.git/info/exclude` (`git status --ignored handoff/` now clean). Nothing was ever tracked, so no commit records the deletion; this handoff is the record. The plan file under `~/.claude/plans/` is outside the repo and was left alone.
- Verified before the PR: the `PROMPTS.md` diff hunks fall entirely inside the `## Session — S6` section; the template's fixed scaffolding lines still count template + Session 1 + S6 + S7 + S8; prohibited-term grep (`design/tokens/build.mjs` list) over the added lines clean; no secret shape; every `§n` names a real section.

## Deviated from plan
- None against the plan. Against the previous chore's record: `handoff/chore-prompts-spine-s6.md` described Decisions 2, 3 and 8 and the `/resume` paragraph as they stood in #19; they are now amended, and that file says so.
- The "delete after the S6 PR opens" instruction inside the stale brief was not followed; the owner chose to delete it now so the next session cannot find two competing S6 specifications.

## Deferred
- The earlier brief's "New from S6" open questions travel with the S6 block: resume order = tile order (confirm) and the `<dl>` keys (strike if unwanted) go into S6's handoff; the `media` bucket name is no longer a constant anywhere — `feat/admin-media` names the bucket when it creates it.
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- Unchanged from `handoff/feat-spine-routes.md`: `alternates.canonical` / sitemap / OG / structured data → `feat/recruiter-seo` · `app/not-found.tsx` and error boundaries → Phase 3 · facet as a path segment only if `/[section]` must be static.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — evaluate later; affects brief §4 and the S4 Zod enum (`FACETS` in `lib/content/schema.ts`). Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth.

## Open questions for owner
1. **Make the documented load command true on this machine** (carried): install `libpq` (`brew install libpq`, `psql` on PATH), or amend the CLAUDE.md Database bullet to name the pooler/container transport. Not changed here.
2. **Merged local branches** (carried, count corrected): `git branch -vv` shows `[gone]` upstreams on seven `chore/spine-*` (hosted-apply, hosted-config, hosted-seed, seed-content, s3/s4/s5-closeout), five `feat/spine-*` (ci, schema, linkedin-import, api, routes), plus `chore/prompts-spine-s6` and, once this merges, `chore/prompts-spine-s6-reconcile`. Earlier handoffs said "six chore/* plus feat/spine-routes" — an undercount. Delete or keep? All left in place.
3. **Vercel Protection Bypass** secret for preview smokes (carried) — still yours.

## Next
S6 — `feat/spine-recruiter` on `lane/spine`. After this PR merges: in a fresh session, `git fetch origin`, `git switch feat/spine-recruiter`, `git merge --ff-only origin/lane/spine`, then paste the `## Session — S6` block from `PROMPTS.md` (the amended one). Not started.
