# Handoff — chore/tooling-posture

**Merged into:** `main`
**Plan row:** none — a `chore/*` branch, cut from and merged into `main` per `BUILD_PLAN.md` §2, because no lane is live and `main` holds the documents it corrects. Documentation only: no app code, no `lib/`, no schema, no migration, no seed, no dependency, no install. It records the tooling, review-loop and licensing posture settled in conversation, which existed in no document and which each of the four remaining sessions would otherwise have re-derived, and it records the Dependabot resolution.

## Shipped

- **`PROMPTS.md` → `## Tooling posture`**, placed after `## Pair 2 — motion notes` and before `## Stop rule`, no `###` headings, prose with reasons rather than a bare list. It belongs in this file because `BUILD_PLAN.md` §5 already names it as the home for "the session guidance, tooling and capability limits", and deliberately not in `CLAUDE.md`, whose remit every session block's pasted precedence paragraph confines to repo conventions. Five blocks: **agents** (Claude is the only agent and the leader; subagents, workflows and deep-research are off unless the owner asks in that session; §2's "one agent per lane" holds regardless; Codex is not wired into this harness) · **Gemini Pro as a second gaze** (a manual paste loop, not an integration — one checkpoint per sub-branch, after the diff is complete and the local gate suite is green, before the PR opens; the ask must be self-contained because it has no repo context; and its three rules — advice not authority, a disagreement is resolved by the document and a genuine gap is a §2.1 halt, and it is never credited in a commit message, PR title or PR body) · **skills and plugins** (five evaluations, below) · **simplicity** (the one additive rule, with its own non-application to this repo's docblocks) · **what was declined as duplication**, named so it is not proposed again.
- **The five skill and plugin evaluations, recorded as posture rather than installs — nothing was installed.** `anthropics/claude-plugins-official`: `/plugins` is first-party and ordinary; `/external_plugins` is held to the bar of a runtime dependency, because a plugin shipping a `.mcp.json` grants that server ambient access to the git-ignored `.env`, which holds `SUPABASE_DB_PASSWORD`, `SUPABASE_ACCESS_TOKEN` and a hosted `DATABASE_URL`. `anthropics/skills`: first-party, `./spec` and `./template` are the reference if a local skill is ever packaged; its `docx`, `pdf`, `pptx` and `xlsx` skills are **source-available, not open source**, and the `pdf` skill carries a scope trap — it invites a resume PDF, which is not in `feat/recruiter-resume-print`'s row and would be scope absorbed mid-flight. `kepano/obsidian-skills`: skipped, because Obsidian Flavored Markdown does not render on GitHub and the handoff family is how the next session gets its context; its `defuddle` skill may be worth revisiting for §9's terms pages when `lane/ingestion` opens, noted in the same breath as the fact that defuddle would strip the stylesheet content the Pair 2 "read a live page's CSS" technique depends on. `multica-ai/multica`: not for this project — it duplicates in a service what this repo does in git, and its self-directed agents plus cron autopilots are the deliberate opposite of §2.1; the bottleneck is §8's owner decisions, not agent throughput. `DietrichGebert/ponytail`: the closest fit; if installed, user scope not project scope, `lite` never `ultra`, the `ponytail:` marker unused (a marker harvested by a slash command is a parallel ledger, and `## Deferred` is this repo's single one), its subagent injection off, and its greenfield benchmark discounted to the low end of its range for convention-heavy work.
- **`CLAUDE.md` — one line**, appended to the Stack paragraph beside the existing "Never commit binaries" rule and in the file's own register: no third-party code under a licence incompatible with MIT enters the tree; skills and plugins are installed, never vendored, and `anthropics/skills`' `docx`, `pdf`, `pptx` and `xlsx` are the named source-available case. The posture itself is not here, for the reason above.
- **The `gh` token memory, outside the repo, corrected by deletion.** `gh auth status` reports `admin:public_key, gist, read:org, repo, workflow` — **`workflow` is present**, so the memory asserting its absence was false. The branch taken was therefore deletion, not amendment: `memory/gh-token-lacks-workflow-scope.md` removed and its pointer line struck from `memory/MEMORY.md` (14 pointers → 13). No corrected version was left behind, because what it recorded was a temporary state of a token rather than a durable property of this project. This is the only part of the session that touches nothing in git, and nothing in a diff, a CI job or a future prompt would ever have caught it.
- **Everything below was run on the final tree; the numbers are measurements, not estimates.**
- `npm run lint` **0 problems** · `npm run typecheck` clean · `npm test` **182 passed / 18 files**, unchanged from the `chore/spine-loose-ends` baseline · `npm run tokens:check` ok. **`npm run build` was deliberately not run** — this branch changes three Markdown files and no code, so a build proves nothing a docs diff does not; it is named here rather than skipped silently. **#28 and #29 changed `.github/workflows/ci.yml`, and no local command behaved differently from the baseline** — the workflow bumps move the runner's action versions, not anything `npm` invokes.
- **Prohibited-term sweep, by hand.** `design/tokens/build.mjs:302` defines `PROHIBITED_TERMS` as two case-insensitive regexes and applies them to SVGs, `tokens.css`, the chime files and the three generated outputs — **never to prose**, so CI could not catch a slip in any of the three files this branch touches. Both were read programmatically out of `build.mjs` rather than typed, and run over the staged diff: **0 matches in added lines** across 78 added lines. **This sweep is the reason to run it rather than assume it:** the first draft of this very bullet quoted the regex literal and tripped its own check, which is also why the sweep must run against a *staged* diff — `git diff main...HEAD` on an uncommitted tree reports 0 added lines and passes vacuously.
- **The standing "three repo hits" figure is an undercount, corrected here rather than in the file that carries it.** Measured on this tree outside `design/`: the first regex matches **17 lines across 12 files** and the second **5 lines across 5**, not three. Every one is constraint text or enforcement code, so nothing is a violation — brief §2.1 (4 lines), `BUILD_PLAN.md` §3 and §7, `CLAUDE.md`'s IP bullet, `PROMPTS.md`'s IP warning, six handoffs, and `chore-post-gate-closeout.md:31`, which quotes the regex literal outright. **Two of them are a second enforcement point nothing in the handoff chain has ever named:** `scripts/linkedin/import.mjs:26` defines its own `PROHIBITED` guard over the generated seed, and `scripts/linkedin/sql.test.ts:101` asserts it. So prose is unguarded, but the *generated seed* is guarded twice over — worth knowing before anyone concludes from the standing prose that `build.mjs` is the only check in the repo. No secret shape, no binary, no third-party asset, no dependency change — `package.json` and `package-lock.json` are not in this diff.

## Deviated from plan

- **The session brief's premise about Dependabot was wrong in one half, and the correction is recorded here rather than written back into the older file.** The brief stated that #28 and #29 had both been merged by the owner between sessions. **Only #29 had** — `actions/checkout` 5→7, squash `bcdb8b8`, 2026-08-30 04:40:17 UTC. **#28 was still open**, with 10/10 checks passing, `MERGEABLE`, and blocked by `REVIEW_REQUIRED`. Dependabot had force-pushed its branch onto the new `main` after #29 landed, so its green run already tested `setup-node` 7 alongside `checkout` 7.
- **The scope refresh did take; the residual block was a different thing entirely.** `gh auth status` reports `workflow` in the token's scope list, so the OAuth-app workflow-file restriction recorded in `handoff/chore-spine-loose-ends.md:28` is genuinely gone. What still held #28 was `main-protection`'s approving-review requirement — the same self-approval wall as PRs #26, #34 and #35, and not a misconfiguration. The two failures look alike from the outside and are not the same failure; conflating them would have sent the owner back to `gh auth refresh` for a problem that command cannot fix.
- **#28 was merged this session on the owner's decision:** `gh pr merge 28 --squash --admin` → squash **`54c3473`**, 2026-08-30 04:51:47 UTC. `main`'s own push-triggered CI on that commit is **green across all 8 jobs** (Lint, Typecheck, Test, Design tokens check, Build, Lighthouse budget, Database migrations & RLS, `CI green`), which is the only thing that proves the `setup-node` 7 + `checkout` 7 combination works on `main` rather than on a PR branch. This branch was cut from `54c3473`, after that run went green. Doing it now rather than later also avoids rebasing an in-flight lane, which is what `chore-spine-loose-ends.md:28` recommended.
- **All seven Dependabot PRs from the gate (#27–#33) are now resolved except #31 and #32.** Merged: #27 (`actions/upload-artifact` 4→7), #29, #30 (the minor-and-patch group: `next` 16.3.2 → 16.3.3, `eslint-config-next`), #33 (`@types/node` 20 → 26) and now #28. Left open and untouched: **#31** (`typescript` 5.9.3 → 7.0.2) and **#32** (`eslint` 9.39.5 → 10.9.1), both `Lint`-red majors needing a code change rather than a rubber stamp.
- **`handoff/chore-spine-loose-ends.md` was left untouched**, including line 28's now-false "could not be merged" and its Open question 1. That is the `handoff/chore-spine-s8-closeout.md:31` precedent — this repo records rather than rewrites a prior handoff's facts — and the last chore followed it four times. The correction is in the stale-prose list below instead.

## Deferred

- **Prohibited terms are still not enforced in prose.** `design/tokens/build.mjs`'s `PROHIBITED_TERMS` scans SVGs, `tokens.css`, the chime files and the generated outputs, never Markdown, so brief §2.1's rule about UI copy and metadata holds by review and not by CI. Checked by hand here across all three changed files: **0 matches in added lines**. Unchanged as a gap.
- **Security headers are still unset** — `next.config.ts` carries only `outputFileTracingIncludes`; no CSP, `X-Frame-Options`, `Referrer-Policy` or `Permissions-Policy`. Phase 3. Rate limiting stays `lane/reactions`.
- **`app/global-error.tsx` is still not written** — the only thing that catches a root-layout throw, and it must render its own `<html>`/`<body>`, so it can reuse none of `site.module.css`. Phase 3, with the security headers.
- **`lane/spine` still cannot be deleted from origin** and survives at `cb11f9f`; the `lane-protection` ruleset's `deletion` rule has an empty bypass list. Because the gate was a squash it is **not an ancestor of `main`**, so ancestry checks mislead — match a branch to its merged PR by `headRefName` and test that PR's `mergeCommit.oid`.
- **Nothing in this section was installed**, and the posture above is not an install plan. `ponytail` in particular is written as *terms if installed*, not as a decision to install — see Open questions.
- **Stale prose left as written, recorded not edited** (the `handoff/chore-spine-s8-closeout.md:31` precedent), carried forward with two entries added: **`chore-spine-loose-ends.md:24` and `:40` both say "the three repo hits in `CLAUDE.md` and `PROMPTS.md`" — the real figures on this tree are 17 lines across 12 files for the first regex and 5 across 5 for the second, all constraint text or enforcement code, two of them a second guard in `scripts/linkedin/`** · **`chore-spine-loose-ends.md:28` says #28 and #29 "could not be merged" and names the missing `workflow` scope as the cause; the owner refreshed the scope, #29 merged at `bcdb8b8` and #28 at `54c3473`, and the residual block on #28 was the review requirement, not the token** · `chore-spine-s6-closeout.md:27` holds the last unstruck `trophy states → S8` copy · `chore-spine-s6-closeout.md:39` (open question 6) still says S7's `[OWNER: …]` fields are unfilled, in tension with line 42 of the same file · `chore-spine-s8-closeout.md` open question 5 says the unset Vercel Protection Bypass secret leaves *previews* SSO-gated, which understates it — Production is gated too · `BUILD_BRIEF.md:268` still lists the domain as open where plan §8 struck it settled.
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin` · media `<figure><img alt>` from the public bucket once it exists → `feat/admin-media`.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — affects brief §4 and `FACETS` in `lib/content/schema.ts`. Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth. The trophy case feels this directly, since its state is read straight off `status`.

## Open questions for owner

1. **Whether `BUILD_PLAN.md` §7 box 18 should now be ticked, and with what wording.** Unresolved since `chore/spine-loose-ends`. It is materially closed for `/[section]` and for every 404, unchanged for `/[section]/[slug]` (SSG-on-demand returns a bare `Internal Server Error` and never reaches `error.tsx`), and was already true for the static routes. The honest tick is a qualified one.
2. **The JavaScript-off blank body on 404 and 500 responses in Next 16.3.3.** Unresolved since `chore/spine-loose-ends`. "Recruiter path works with JS disabled" is a spine acceptance item that holds for every 200 page and does not hold for an error page. The options remain: accept it, re-test after a Next upgrade, or make Phase 3's hardening own it.
3. **The site is still not publicly live**, and both causes are yours: Vercel Deployment Protection covers Production as well as previews, and `kerwynjean.dev` is not wired (HTTPS fails, DNS on Porkbun parking). ⚠ Vercel SSO returns a **200 on every path** — grep the body for `<title>Login – Vercel</title>` before believing a status code.
4. **New — do you want `ponytail` installed, on the terms in the Tooling posture section?** User scope not project scope, `lite` never `ultra`, the `ponytail:` marker unused, and its subagent injection turned off. Nothing was installed; the section records the terms so the decision is a yes or a no rather than a re-evaluation.
5. Carried forward and still unresolved, unchanged from `chore/spine-loose-ends` rather than dropped: **the Supabase integration's first run is still unverified** (the `Supabase Preview` check has an empty output body, so "nothing to apply" is consistent with it rather than proven by it; the dashboard integration log is the only place that states it) · **`psql` / libpq is still not installed on this machine**, so the hosted-seed chore in `CLAUDE.md`'s Database bullet cannot be run as written, unchanged since S3 and blocking nothing today · **resume order is tile order, not a strict date sort** (`compareRecency` — featured, then `sort_weight`, then most recent start) · **the `<dl>` keys on the entry page** — `cause` (experience), `activities` (education), `credential_id` (certification) · **bot protection is specified nowhere**, which becomes a real question when `lane/reactions` opens the first public write path, and where CAPTCHA would conflict with brief §2.3.
6. **This PR cannot auto-merge and never could.** `main`-protection requires an approving review and GitHub refuses to let anyone approve their own pull request, so merging is your `--admin` call — the same as PRs #26, #34 and #35, and not a misconfiguration.

## Next

**Phase 1, Pair 1: `feat/recruiter-resume-print`**, cutting **`lane/recruiter` from `main`** first (never from `lane/spine`, which survives at `cb11f9f` and is not an ancestor of `main` because the gate was a squash). A new `lane/*` inherits `lane-protection` automatically, so sub-branch → lane auto-merges on green CI with no human step. Its full session block is already committed in `PROMPTS.md` → `## Pair 1 — lane/recruiter` as **Session A**, and the lane-level prose *before* the block must be read first, because its two traps are the point: **`next/og` defaults to the Edge runtime, where `node:fs` does not exist**, while `app/(explorer)/tiles.ts` and the KJ badge read in `app/page.tsx` both use a literal `readFileSync` — an ENOENT in the deploy and nowhere else, since `next start` and Lighthouse both run from the repo root; and **Lighthouse's SEO category already scores 1.00 and will keep scoring 1.00** with no sitemap, no OG image and no JSON-LD, so `feat/recruiter-seo` has no safety net and every acceptance item on it is verified by hand or not at all. The lane is blocked on nothing — both of its §8 rows are settled. **Session B, `feat/recruiter-seo`**, is the bulk of the lane and gets a session of its own. `lane/ingestion` still has no block on purpose: it waits on §8's Steam profile visibility and on §9's verify-at-build-time items. Pair 2 runs only after Pair 1 merges. Not started.
