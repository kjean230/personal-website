# Handoff — feat/spine-linkedin-import

**Merged into:** `lane/spine`
**Plan row:** BUILD_PLAN §4, S3 — LinkedIn export parser → `entries` rows · output committed as editable seed data · owner edits before S6

## Shipped
- `scripts/linkedin/` — the one-shot importer, zero new dependencies, `.mjs` with `// @ts-check` JSDoc types (typechecked through the tests): `csv.mjs` (RFC 4180 parser: quotes, doubled quotes, newlines inside quotes, BOM, CR/LF/CRLF; ragged records throw), `uuid.mjs` (name-based v5-shaped ids from a fixed namespace + the export row's natural key, so ids survive re-runs and slug edits), `supplement.mjs` (the **only** hand-entered facts, each with its source), `normalize.mjs` (pure rows → records: kinds, facets, dates with precision, bullets → markdown, entity decoding, slugs, review notes), `sql.mjs` (records → editable SQL), `import.mjs` (CLI). 46 unit tests on inline fixtures; the real export is never read by a test. `npm run linkedin:import -- <dir> [--force]`; it refuses to overwrite the seed without `--force`.
- **Verified at build time:** the export is a directory named `Complete_LinkedInDataExport_08-23-2026.zip/`; it has **no `Certifications.csv`**. The importer opens exactly `Positions.csv` (9 rows), `Projects.csv` (2), `Education.csv` (2), `Honors.csv` (4), `Volunteering.csv` (1) and nothing else (the export also holds connections, messages, contact details). `Profile.csv`, `Skills.csv`, `Courses.csv` are untouched (below).
- `supabase/seed.content.sql` — 19 entries (10 experience, 2 project, 2 education, 5 certification), 2 live relations, 1 link, 1 suggested relation commented out. Every row: fixed id, `metadata.source`, `metadata.date_precision` (LinkedIn dates are months; day = `01`), a comment naming its export row and its `REVIEW` items. Entries and links upsert on id with a `where … is distinct from` guard (a no-op apply touches no `updated_at`); relations are written by slug via scalar subselects, so a missing slug is a `23502` and the seed fails loudly. Bodies are dollar-quoted markdown, editable without escaping. Scanned: no email, phone, address, connection, or prohibited term.
- **The brief §4.1 record, real:** `break-through-tech` (experience, facet `research`, current) ← `airbnb-superhost-classifier` `part_of` ← `machine-learning-foundations` `certifies` (certification, status `unlocked`, eCornell, issued 2026-08-05, `links` row to the credential). Title, issuer and date of the certification were read from the public page of the credential URL you supplied — the export cannot name it.
- `supabase/tests/content.test.ts` — 9 integration tests: BTT is one experience/research row; surfaces under Experience by kind, under Projects via a `part_of` child that is a project, in the trophy case via exactly one unlocked certification with its credential link; all three placements resolve to one id and no twin exists; every content row names its source and none carries `fixture`; fixture and content are disjoint; re-applying the seed in a rolled-back transaction changes no count and no `updated_at`; a relation naming an unknown slug fails with `23502`. `npm run db:test` = 40 (31 RLS + 9 content).
- `supabase/tests/rls.test.ts` — the "three trophy states" assertion now filters on fixture rows (it counted every certification; real ones now exist). Every other count was already fixture-scoped.
- `scripts/db/apply.mjs` seeds `SEED_FILES = ["seed.sql", "seed.content.sql"]` in that order (a glob would sort `seed.content.sql` first); `supabase/config.toml` `[db.seed] sql_paths` mirrors it for CLI parity. CI's database job needed no change: `db:apply` now loads both seeds and `db:test` runs both files.
- Verified locally: `npm run db:reset` from scratch → both seeds apply; second `npm run db:apply` → 0 rows with `updated_at <> created_at`; `lint`, `typecheck`, `test` (46), `tokens:check`, `db:test` (40) green.
- `.gitignore`: `Complete_LinkedInDataExport*` guard. `vitest.config.ts` includes `scripts/**/*.test.ts`. `package.json`: `linkedin:import`. CLAUDE.md: commands and the Database bullet.

## Deviated from plan
- None against the S3 row. Two notes on how "entries rows" was read: the seed also carries the two `entry_relations` and the one `links` row the §4.1 shape requires — without them the BTT record has one placement, not three. And honors are `kind = certification` with `metadata.category = "award"` rather than a new kind: brief §5 puts "certifications and awards" in one trophy case and brief §4 lists no `award` kind, so a metadata discriminator keeps S8's trophy query on one kind. S4's Zod schema owns that enum; change it there if you disagree.

## Deferred
- **Loading `seed.content.sql` into the hosted project** — out of S3 scope by the task. Seeds never run hosted (Branching off; the GitHub integration applies migrations only), and previews share the hosted database, so S6's preview renders an empty site until the seed is applied once by hand with service credentials (`psql "$DATABASE_URL" -f supabase/seed.content.sql` from the git-ignored `.env`, after your edits). Natural moment: when the edits below are done, before S6 opens its preview.
- `links.kind` has no `credential` value (closed set: company | repo | paper | demo | profile); the credential link uses `profile`. A one-line migration for whoever needs it first (S4 or `lane/content`).
- `Skills.csv` (61 rows) → `tags`, `Courses.csv` (17, no school association in the export), `Profile.csv` headline/summary (contains your email; never read) → `lane/content` (Pair 3), which owns tags, hobbies, and the remaining entries.
- Metadata keys S4's Zod schemas should know: `source` (`{export, file}` or `{export, url}`), `date_precision` (`day | month | year`), `category` (`certification | award`), `credential_id`, `credential_url`, `location`, `cause`, `activities`.
- `sort_weight` is 0 and `featured` false everywhere; recency comes from `start_date` until you set them.

## Open questions for owner
**Edit `supabase/seed.content.sql` before S6** (BUILD_PLAN §8 "Phase S3 end"). Every item is also marked `-- REVIEW` beside its row. Edit values only — never an id; if you rename a slug, rename it in the relations block too; after deleting or moving a row, `npm run db:reset`.
1. **Slugs (all 19)** — mechanical: company alone when it has one position, else company + title; degree + school for education. Brief §2.2's example is `/experience/guardian`, so you will want shorter ones, e.g. `fordham-university-teaching-assistant-for-information-and-data-management-cisc-2500` → `fordham-ta-cisc-2500`, `fordham-university-graduate-school-of-arts-and-sciences-master-of-science-ms` → `fordham-ms-data-science`.
2. **`summary` (all 19)** — one line for the tile; the export has no such field.
3. **`facet` (12 rows null)** — not in the export: Guardian Life, Fordham Undergraduate Research Assistant, Fordham TA, WCS Data Analyst Researcher, WCS Data Field Researcher, Fordham INSTEP Mentor, Fordham STEP Tutor Counselor, Fordham CSTEP Summer Scholar, both projects, both education rows. Values: `corporate | research | volunteer | classroom | coursework`, or leave null. `break-through-tech` = `research` (brief §4.1) and `brooklyn-public-library-stocker` = `volunteer` — confirm.
4. **`subtitle` (6 rows null)** — organisation for the two projects; issuer for the four honors (CSTEP Scholarship, Fordham Jogues Scholarship, Dean's List, Riversville Foundation Scholarship).
5. **`start_date`** — Dean's List has none in the export (body says "Spring 2024 Spring 2025").
6. **The two live relations** — `airbnb-superhost-classifier part_of break-through-tech` (inferred: only project inside the fellowship's dates; scikit-learn content matches the ML Foundations curriculum) and `machine-learning-foundations certifies break-through-tech`. Delete the line if wrong.
7. **The certification row** — title "Machine Learning Foundations", issuer "eCornell (Cornell University)", issued 2026-08-05, from the credential page. Confirm, and say if the trophy should be titled differently (e.g. the program name).
8. **Suggested relation** — `urban-arthropod-abundance-modeling part_of wildlife-conservation-society-data-field-researcher` is commented out; uncomment to enable.
9. **Honors as `certification` + `category: award`** — confirm you want scholarships and Dean's List in the trophy case; delete rows you do not.
10. **Status and currency** — every row is `status = unlocked`. Consider `in_progress` for the MS (Aug 2026–May 2028) and the BS (ends May 2027); both have end dates so `is_current` is false (the schema forbids both). `break-through-tech` is `is_current = true` with no end date.
11. **`featured` / `sort_weight`** — all false / 0. Set for the tile row.
12. Body of `brooklyn-public-library-stocker` starts with "-I assisted" exactly as exported; tidy if you like.

## Next
S4 — `feat/spine-api` on `lane/spine`: Zod schema per `kind`, query layer for tile row, detail, facet counts, relation traversal, caching and revalidation. The BTT record and the metadata keys above are its inputs. **Done 2026-08-26 (PR #12), see `handoff/feat-spine-api.md`.**
