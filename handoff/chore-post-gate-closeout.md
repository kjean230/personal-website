# Handoff — chore/post-gate-closeout

**Merged into:** `main`
**Plan row:** none — the post-gate close-out. The spine gate merged and no document in the repo knew it: nothing in `handoff/` recorded PR #26, seven `## Next` pointers still read `Not started.`, and `PROMPTS.md`'s gate section was written entirely in the future tense about events that had already happened. This chore makes the documents describe the project's actual state, records the gate where a future session will find it, and writes down the Pair 2 motion direction that until now existed only in conversation. Documentation only: no app code, no `lib/`, no `app/`, no schema change, no migration, no seed change, no dependency.

## Shipped

- **The gate record.** `lane/spine` → `main` merged **2026-08-29 20:52:19 UTC, PR #26, squash `2c80113`**, all 8 CI jobs green (Lint, Typecheck, Test, Design tokens check, Build, Lighthouse budget, Database migrations & RLS, `CI green`) and the Vercel Production deployment successful. The published body carried 17 `[x]` and 1 `[ ]`: box 18 (error boundaries) was deliberately left unticked as a signed exception deferred to Phase 3. **It was merged with `--admin`** — GitHub refuses to let anyone approve their own pull request and the owner is `main-protection`'s sole bypass actor (`bypass_mode: always`). Expect that on every future lane → `main` merge; it is not a misconfiguration.
- **Seven `Not started.` pointers struck** in house style (`~~Not started.~~ — **done …**`), five of which had gone stale *before* the gate and were missed by earlier close-outs: `chore-spine-s8-closeout.md:59` (the gate) · `chore-spine-s6-closeout.md:42` (S7, S8 **and** the gate in one sentence) · `chore-spine-s4-closeout.md:40`, `chore-spine-hosted-seed.md:30`, `feat-spine-api.md:63` (S5, PR #17) · `chore-prompts-spine-s6.md:33`, `chore-prompts-spine-s6-reconcile.md:43` (S6, PR #21). Nothing deleted. `chore-spine-s6-closeout.md:42` also called S7 "**blocked**" on two `[OWNER: …]` fields; the annotation records that the block cleared at the top of the S7 session.
- **`CLAUDE.md:27`** — the Lighthouse audit list said four URLs; `lighthouserc.json` audits **five**. `/certifications` inserted, in the file's own order.
- **`CLAUDE.md:39`** — "when `lane/spine` merges to `main`" became "on every merge to `main`", the standing rule the sentence was always describing, plus the note that the integration's first run is **unverified** (below).
- **`PROMPTS.md` → `## After S8 — the spine gate`** converted in place from prediction to dated record. The heading and the §4 acceptance paragraph are byte-identical, because three handoffs cross-reference this section by its exact text. Each of the four predictions now carries what actually happened.
- **`BUILD_PLAN.md` §2** — `chore/*` documented for the first time. It appeared in **zero** authority documents despite thirteen merged chore PRs, which is exactly why this session had to stop and ask where to branch from.
- **`BUILD_PLAN.md` §5** — the `lane/recruiter` row annotated (two of four sub-branches already delivered, below), and the Pair 2 motion mapping added to the `lane/console-shell` row.
- **`BUILD_PLAN.md` §8** — a `Pair 1` row added for the resume contact block, born settled in the same struck form the domain row already uses.
- **`DESIGN.md`** — a new "Motion is borrowed the same way" subsection under the borrowed-grammar heading, stating the distinction for hover-zoom, morph and console navigation. **No source URLs**, per the standing rule that `DESIGN.md` is a provenance record and not a comparison exhibit.
- **`PROMPTS.md` → `## Pair 2 — motion notes`** — a new section carrying the owner's motion direction, the IP constraint, the harness's lack of video comprehension, the six binding constraints, and how to oversee an implementation agent whose output is motion rather than markup.
- **`REFERENCES.md`** — a `## Motion — Pair 2` section with the eight sources annotated per sub-branch. Git-ignored by design, so it is **not** in this PR; its absence in a fresh clone is normal and is not a blocker.

## Deviated from plan

- **This chore is based on `main`, not on a lane** — the first chore to do so since S1. `BUILD_PLAN.md` §2 named no base for a `chore/*` branch and `PROMPTS.md` describes only a *pre*-gate close-out, so under §2.1 this was a plan-silent ambiguity and the session stopped and asked rather than picking the reading that let work continue. The owner chose `main`. The precedent that settled it: `handoff/chore-prompts-spine-s6.md:16` records that every docs chore since S1 targeted `lane/spine` **because** `main` carried no `.github/`, so the required `CI green` check could never report on a branch cut from it — a constraint that ended when `2c80113` carried the workflows to `main`. `chore/docs-reconcile` (#1) and `chore/references-note` (#2) both merged to `main` before that constraint existed. §2 now records the rule so the next session does not have to re-derive it.
- **The remit grew from three stale statements to seven, by evidence.** The brief named `chore-spine-s8-closeout.md:59`, `chore-spine-s6-closeout.md:42` and `CLAUDE.md:27`. A full sweep of `handoff/` found seven bare `Not started.` pointers and no record of PR #26 anywhere. The owner chose the wider remit.
- **Status was annotated; prior handoffs' *facts* were not rewritten.** Those are different operations with different precedents in this repo — striking a `## Next` pointer is the documented house style, while `handoff/chore-spine-s8-closeout.md:31` deliberately left a wrong count in a prior handoff in place and recorded the correction instead. Everything under Deferred and Open questions below follows the second rule.

## Deferred

- **Security headers are still unset** — `next.config.ts` carries only `outputFileTracingIncludes`; no CSP, `X-Frame-Options`, `Referrer-Policy` or `Permissions-Policy`, and Lighthouse's best-practices score does not audit strictly enough to catch it. Phase 3. A CSP is the belt-and-braces complement to the escaping in `lib/render/markdown.ts`.
- **Error boundaries — this is §7 box 18, merged unticked.** Verified on the merged tree: no `error.tsx`, `global-error.tsx` or `not-found.tsx` anywhere in `app/`, and **zero `catch` blocks in `lib/` or `app/` outside tests**. The queries throw — **4 throw sites in `lib/content/queries.ts` and 1 in `lib/content/schema.ts`** (the incoming brief said 5 and 3; corrected here rather than in it). So a Supabase outage on a cache miss 500s `/[section]`, and because `/resume` prerenders, an outage fails `next build` rather than a request. A `chore/spine-error-boundary` adding `app/error.tsx` + `app/not-found.tsx` would close it and is genuinely small.
- **`lane/spine` could not be deleted from origin** and survives at `cb11f9f`. The `lane-protection` ruleset (id 21349409) carries a `deletion` rule with an **empty** `bypass_actors` list and `current_user_can_bypass: "never"`, so nobody — the owner included — can delete a `lane/*` branch while the rule is active. Harmless to leave, but a trap: because the merge was a squash, `cb11f9f` is **not an ancestor of `main`** (`1 20` divergence), so `git merge-base --is-ancestor` will answer "no" for a branch that demonstrably landed. Clearing it needs a ruleset change — do not make one without asking.
- **Prohibited terms are not enforced in prose.** `design/tokens/build.mjs`'s `PROHIBITED_TERMS = [/nintendo/i, /joy-?con/i]` is applied to SVGs, `tokens.css`, the chime files and the three generated outputs — **never to `DESIGN.md`, `PROMPTS.md`, `BUILD_PLAN.md` or `CLAUDE.md`**. Brief §2.1's rule about UI copy and metadata therefore holds by review, not by CI. Checked by hand for this chore, which added IP prose to two of those files.
- **Dependabot.** Seven PRs open (#27–#33). Five are green; **#31** (`typescript` 5.9.3 → 7.0.2) and **#32** (`eslint` 9.39.5 → 10.9.1) are **CI-red on `Lint`** and cannot merge — major bumps needing a code change, not a rubber stamp. Two *update jobs* also failed, for `tmp` and `uuid`: both are deep `@lhci/cli` transitives Dependabot cannot bump independently, dev-tree only, zero runtime impact — not a repo defect. Four Dependabot security alerts are open, consistent with the S8 audit (10 findings, all in the `@lhci/cli` dev tree, 0 runtime).
- **`PROMPTS.md` has no Pair-1 block**, though `PROMPTS.md:122` records that from S6 on each session's block is committed before it runs. This chore added a **Pair-2** block because that material would otherwise have been lost, which leaves the two out of order. A Pair-1 block needs content decisions not in hand, so it is flagged rather than invented — see Open questions.
- Unchanged from earlier handoffs: delete `.github/workflows/keep-warm.yml` when Pair 1's ingestion cron lands · `links.kind = 'credential'` migration · facet-count view · generated-types drift check in CI · `Skills.csv` / `Courses.csv` / Profile → `lane/content` · admin `updateTag` / `revalidateContent()` wiring → `lane/admin`.
- Unchanged from `handoff/feat-spine-routes.md`: `alternates.canonical` / sitemap / OG / structured data → `feat/recruiter-seo` (needs `metadataBase` = the domain, plan §8) · `app/not-found.tsx` and error boundaries → Phase 3 · facet as a path segment only if `/[section]` must be static.
- **Stale prose left as written, recorded not edited** (the `chore-spine-s8-closeout.md:31` precedent): `CLAUDE.md:21` says a migration "that has reached `lane/spine` is never edited" — the boundary that matters from here is `main`, and as written a future agent on `lane/recruiter` gets no rule at all · `chore-spine-s6-closeout.md:27` holds the last unstruck `trophy states → **S8**` copy · `chore-spine-s6-closeout.md:39` (open question 6) still says S7's `[OWNER: …]` fields are unfilled, which now sits in tension with this chore's annotation on line 42 of the same file · `chore-spine-s8-closeout.md` open question 5 says the unset Vercel Protection Bypass secret leaves *previews* SSO-gated, which is now understated — Production is gated too · `BUILD_BRIEF.md:268` still lists the domain as open where plan §8 struck it settled.
- **Owner notes carried forward, unchanged, not acted on:**
  - Whether `education` earns a sixth facet value — affects brief §4 and `FACETS` in `lib/content/schema.ts`. Both education rows keep `facet = null` and their `-- REVIEW` note.
  - Coursework (CS/DS classes) as their own entries — `lane/content`.
  - An admin control to flip `status` / `end_date` / `is_current` when the degrees and BTT finish; `is_current` and `status` overlap and one should be named the source of truth. The trophy case feels this directly, since its state is read straight off `status`.

## Open questions for owner

1. **The site is not publicly live, for two independent reasons.** (a) **Vercel Deployment Protection covers Production**, not only previews — every Production host 302s to `vercel.com/login` and serves `<title>Login – Vercel</title>`. (b) **`kerwynjean.dev` is not wired** — HTTPS fails outright and DNS still resolves to Porkbun parking. Both are yours: the protection setting in the Vercel dashboard, and the domain attach plus DNS records. ⚠ **Vercel SSO returns a 200 on every path, `/nonexistent` included** — grep the body for that title before believing any status code from a Vercel host. Do not create Porkbun's Let's Encrypt certificate; Vercel issues and renews its own.
2. **The Supabase integration's first run is unverified.** The `Supabase Preview` check reported success at the gate but carries an **empty output body** (read back through the check-runs API, not inferred from the UI), so "nothing to apply" is *consistent with* that result and *not proven by* it. Both migrations were already pushed by hand and are in `supabase_migrations.schema_migrations`. The dashboard integration log is the only place that states what it actually did — read it before a migration relies on this path.
3. **Two `lane/recruiter` sub-branches are already built**, so the lane is half the size the plan implies. `feat/recruiter-sections` was delivered by S6 + S8 and `feat/recruiter-facets` by S5 + S6 — facet counts are live from `getFacetCounts` and never hardcoded, the "All" chip is present and first, active state is `aria-current="page"` plus real CSS, and empty sections degrade to `All (0)` under exact-array assertions. §5 is annotated accordingly. The lane's real work is `feat/recruiter-resume-print` (zero `@media print` anywhere in the repo today) and `feat/recruiter-seo` (no `sitemap.ts`, `robots.ts`, OG route, or `ld+json` anywhere; `metadataBase` lands there and nowhere else). Optional polish left unclaimed: facet icons and an "Other" chip for the computed-but-unsurfaced `count.unfaceted`.
4. **A Pair-1 block for `PROMPTS.md`** — see Deferred. Say the word and it can be written before `lane/recruiter` opens, which is what `PROMPTS.md:122` expects.
5. **Should the stale prose under Deferred be corrected as policy?** This repo's precedent is to record rather than edit a prior handoff's facts, and this chore followed it. `CLAUDE.md:21` is the one that is genuinely load-bearing rather than merely historical.
6. **`psql` / libpq is still not installed on this machine**, so the hosted-seed chore in `CLAUDE.md`'s Database bullet cannot be run as written. Unchanged since S3; blocks nothing today.
7. **The dev server on port 3000 is still yours to restart** (`npm run dev`). This session started nothing.
8. **Resume order is tile order, not a strict date sort** — `compareRecency` (featured, then `sort_weight`, then most recent start), the same order the section pages use, so the two never disagree. Unchanged from S6, S7, S8.
9. **The `<dl>` keys on the entry page** — `cause` (experience), `activities` (education), `credential_id` (certification). Unchanged from S6, S7, S8.
10. **Bot protection is specified nowhere.** It becomes a real question when `lane/reactions` opens the first public write path, and CAPTCHA would conflict with brief §2.3.

## Next

**Phase 1, Pair 1: `lane/recruiter` + `lane/ingestion`** (BUILD_PLAN §5), two concurrent lanes at most, disjoint by file — app routes versus `.github/workflows` and worker scripts. Both lanes are cut from the new `main` (`2c80113`), never from `lane/spine`, and a new `lane/*` branch inherits `lane-protection` automatically, so sub-branch → lane auto-merges on green CI with no human step. `lane/recruiter` is blocked on nothing: its §8 items are settled (the domain, and now the resume contact values). `lane/ingestion` still waits on plan §8's **Steam profile visibility**, and on plan §9's verify-at-build-time items — Vercel Hobby non-commercial terms, Supabase free-tier pause behaviour, and current Spotify / Steam / IGDB API access terms including IGDB cover-art usage rights before any box art is displayed. Recommended first sub-branch is **`feat/recruiter-resume-print`**: it is unblocked, self-contained, owns the repo's first `@media print` styles, and is the smaller of the lane's two real rows — leaving `feat/recruiter-seo`, the bulk of the lane, for a session of its own. Pair 2 (`lane/console-shell` + `lane/admin`) runs only after Pair 1 merges, because Play Activity depends on ingested data existing; its motion direction is recorded in `PROMPTS.md` → `## Pair 2 — motion notes`, `BUILD_PLAN.md` §5, `DESIGN.md`, and the git-ignored `REFERENCES.md`. Not started.
